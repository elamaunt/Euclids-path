/-
  Root module `Euclids-path`. Imports go IN THE ORDER OF THE PROOF'S PATH
  (Euclid's engine → its laws → reduction to twins → attack lines → final node SNOL).

  Path map: prose/00_ProofPath.md. Paired prose for each module — prose/NN_*.md.

  Status legend:
    * proven modules — without `sorry` (standard axioms);
    * the single open goal — `Step00.twin_prime_conjecture` (= the final node SNOL);
    * the single explicit input to closure — `SNOL.SNOLInput` (also `ToTwins`-`H`).
  `sorry` cannot be forged: a "green" file = the genuinely verified part.
-/

-- 00. Foundations: goal and base definitions (TwinLowers, IsTwinCenter)
import EuclidsPath.Step00_Overview

-- I. Euclid's engine and its laws (atomic, proven)
import EuclidsPath.Engine.EPMI              -- impossibility of a perpetual engine
import EuclidsPath.Engine.Carrier           -- shared gcd ∣ 2 (carrier of two)
import EuclidsPath.Engine.TwoGap            -- XY−ZW=2 (conservation of the two, 1st law)
import EuclidsPath.Engine.Descent           -- strict descent + boundary-law
import EuclidsPath.Engine.Irreversibility   -- will not turn back / asymmetry / turn⇒halt (2nd law)
import EuclidsPath.Engine.NoBackward        -- the diagonal vanishes (exclusivity)
import EuclidsPath.Engine.Squeeze           -- cubic squeeze (short train)
import EuclidsPath.Engine.BK                -- bounded additive cycle
import EuclidsPath.Engine.Cycle             -- factor-repeat rigidity, cross-side fuel

-- II. Reduction of the twin conjecture to the block core
import EuclidsPath.Engine.NonCover          -- survivor ⇒ twin; infinite_of_unbounded_centers
import EuclidsPath.Engine.TwoTransport      -- twin_prime_conjecture_of_blocks

-- III. Attack lines on the estimate (studied along the way; hit the parity wall)
import EuclidsPath.Engine.FourCorner        -- N₃₃<N₀₀ from four-corner + side-corner
import EuclidsPath.Engine.ModelFourCorner   -- 20·C(n,6) ≤/< C(n,3)² (model, strict)
import EuclidsPath.Engine.RealFourCorner    -- exact decomposition of the remainder
import EuclidsPath.Engine.ToTwins           -- twin_primes_of_four_corner (conditional on H)
import EuclidsPath.Engine.FiniteContradiction -- finite ∧ H ⇒ False (3 routes by contradiction)
import EuclidsPath.Engine.PaymentLedger     -- channel/tax/shifted-primorial/Y_A (payment law)

-- IV. Final node: the single open lemma
import EuclidsPath.Engine.SNOL              -- rank descent → rank-1 → shifted-neighbour (SNOL)
import EuclidsPath.Engine.OldPeel           -- SNOL unfolds into old-peel ⇒ EPMI contradiction
import EuclidsPath.Engine.GenealogyForest   -- 🟢 genealogical forest: descent sinks are EXACTLY twins; every center descends to a twin below (honest: downward only)
import EuclidsPath.Engine.NOPSL             -- NOPSL: no old-peel sink ⇒ twin sink (closure)
import EuclidsPath.Engine.Regeneration      -- Old-Peel Regeneration: dichotomy Ω_A (cases 1–3)
import EuclidsPath.Engine.Residuals         -- Step00 residuals: start (primorial), sink⇒twin, height
import EuclidsPath.Engine.RigidClose        -- rigid-closure: reaches_twin without a cycle + cofactor_is_center
import EuclidsPath.Engine.CleanGraph        -- clean/boundary split: clean-sink ⟹ twin above M₀ (correction)
import EuclidsPath.Engine.BoundaryDecomp    -- BoundaryExit decomposition + global absorber node
import EuclidsPath.Engine.PumpFinal         -- pump (conditional) + EPMI break on a self-loop
import EuclidsPath.Engine.PumpStanding      -- pump v2 (two-token standing cycle) + EPMI break remains
import EuclidsPath.Engine.RiemannBranch     -- side branch: RH by contradiction through the engine (conditional)
import EuclidsPath.Engine.RiemannLiouville  -- RH via Liouville-equivalence (λ=(−1)^rank)
import EuclidsPath.Engine.ProductHall       -- ProductHall/Steering pump (4-case logic, without circular payment)
import EuclidsPath.Engine.SeparatingScale   -- separating scale: ¬ProductHall by pure arithmetic (node closed)
import EuclidsPath.Engine.RankDescent       -- energy-defect trichotomy + finite product-rank descent
import EuclidsPath.Engine.ProductCore       -- corrected product-core: 3 defects closed by theorems
import EuclidsPath.Engine.CarrierBridge     -- the last link: carrier is infinite; input = FactorizationData
import EuclidsPath.Engine.MkNode            -- mkNode: RankNode from a composite side (arithmetic, proven)
import EuclidsPath.Engine.LabelledFanIn     -- labelled fan-in: König PROVEN, SNOL reduced (v2→v4)
import EuclidsPath.Engine.AtomicSNOL         -- post-audit: legal subtype (no hAll) + SNOL-free atomic + SNOLDeriv (R8 = red line)
import EuclidsPath.Engine.ConcreteComponents -- discharge active/old-peel components from separating scale; localization of the counting wall
import EuclidsPath.Engine.BadCoverDescent    -- bypass of the counting wall: bad-cover finite descent (conditional reduction, R3 = new red line)
import EuclidsPath.Engine.ObstructionClosure -- abstract well-founded obstruction-engine; inputs are NOT instantiable (SNOL.bad — counting)
import EuclidsPath.Engine.ManyUnresolved     -- many-unresolved collision route: combinatorics proven, U4-terminal is circular
import EuclidsPath.Engine.HigherEnergy       -- weighted debt energy: a real well-founded engine; input = step00_promotion_is_weightedDebtReplacement
import EuclidsPath.Engine.HigherTower        -- Euclidean Tower: fixed-center BadTower is VACUOUS; moving tower = orientation wall
import EuclidsPath.Engine.EngineTower        -- EngineTower without traversal: the recurrence branch is ALSO vacuous; escape/collision = counting wall
import EuclidsPath.Engine.ParityBarrier       -- the parity wall as a THEOREM: a finite-sieve does not certify twin; intersection requires cofinal
import EuclidsPath.Engine.ReverseTower        -- reverse engine: ancestor tree + barrier (abstract no-go); Step00-bridges = wall
import EuclidsPath.Engine.AboveConflict       -- conflict in "Above": order-logic is trivial; step00_forces_above_conflict is trapped (machine-wise)
import EuclidsPath.Engine.JumpBarrier          -- jump/cut-barrier: paid jump + cofinal cut-pigeonhole (proven); force-ray/barrier = inputs (wall)
import EuclidsPath.Engine.PaidDynamics         -- paid dynamics: no free inertia/acceleration/cloning (proven); regeneration-to-close = SNOL wall
import EuclidsPath.Engine.ClosedUniverse       -- the engine does not leave the universe: universe-preservation + closed-paid no-run (proven); promotion_paid_or_closes = wall
import EuclidsPath.Engine.BoundaryDefectPayment -- boundary-defect: extraction of SmallPrimeDefect + impossibility of no-tax payment (proven); input = dichotomy cycle∨payment
import EuclidsPath.Engine.BoundaryLedgerCollision -- ledger-collision: pigeonhole + burning the payment ⟹ cycle (proven); input = collision-resolve
import EuclidsPath.Engine.ConcreteStep00Graph  -- CONCRETE graph 6m±1 (not the abstract σ); lexRank height PROVEN (drops on absorb); remainder = ledger/family/resolve
import EuclidsPath.Engine.TwinNodeEpistemic -- 🟢 epistemic complement of the NODE: refuting twins = an unconditional supply (twinBound_carries_unbounded_supply) ⟹ an engine in a stable universe; strict narrowing of the node to A ≥ 5; twinNode_unknowable; taint does not grow
import EuclidsPath.Engine.TwinScaleAdvances -- 🟢 scale advances of the node: small scale A ≤ 4 is dead for ALL bases M0 ≥ 1 (no_projection_resolves_at_smallScale_allM0, 5-adics+pigeonhole); a growing separating scale (exists_growing_separating_scale, Bertrand+primorial, step (1) of the plan §29.6); the §29.5 barrier is machine-visible (fixedWindow_factorization_impossible_at_separating_scale); skeleton bridge BoundaryDecomp↔CarrierBridge; taint does not grow
import EuclidsPath.Engine.RiemannEngine        -- RH→OffCriticalZero→Engine (pure form); bridges = inputs; RH is not proven
import EuclidsPath.Engine.RiemannImpossibleEngine -- RH via an impossible closed-paid engine: no_riemannEngineFactory is UNCONDITIONAL; input = strip-bridge
import EuclidsPath.Engine.RiemannImpossibleEngineOff -- OffCritical version: removes TrivialBelowZeroClassification; the SINGLE input = OffCriticalRiemannEngineBridge
import EuclidsPath.Engine.RankJumpBridge        -- rank/parity bridge (Liouville λ=(−1)^Ω ↔ factory): everything AROUND the node is proven; §4 ban on cheating; input = RankJumpLocalization
import EuclidsPath.Engine.DichotomyEngine       -- UNIFICATION: Close∨paid-step ⟹ Close is inevitable (close_forced); all cheating regimes absorbed; input = the local dichotomy
import EuclidsPath.Engine.DissipativeCascade    -- cascade-certificate (Step00/RH/NS-analogy): capacity/overflow decomposition of the Liouville node; ℝ vs ℕ warning
import EuclidsPath.Engine.NavierStokes          -- the NS equation ITSELF (PDE-predicate, mathlib fderiv/gradient/∫): momentum+incompressible; zero-solution; cascade-linkage
import EuclidsPath.Engine.RiemannRankProjection -- live node No. 8 of the RH-branch: strict+gradual decomposition unpaired_gives_jump (first-crossing PROVEN); junction with TwinCarrierPairing
import EuclidsPath.Engine.RiemannTrivialZeros -- INPUT No. 1 CLOSED: classification of zeros Re≤0 PROVEN (functional equation); RH is now conditional ONLY on EngineBridge
import EuclidsPath.Engine.RiemannRankProjectionAudit -- ⚠️ vacuity No. 2: the goal of route No. 8 is not anchored; the honest wall = ¬LiouvilleViolation; window-bookkeeping closed
import EuclidsPath.Engine.RiemannTwoTransportFront -- input No. 1 in two-transport form: split+realization+builder; HONESTY: coherent bridge ⟺ RH (circularity is inherited)
import EuclidsPath.Engine.RiemannArithmeticTwoTransport -- NON-engine goal: qrs = abv + 2 (gap 2 proven); HONESTY: with a free anchor both inputs = a single question AdmissibleAtomExists
import EuclidsPath.Engine.RiemannSpectralAnchorAudit -- collapse audit formalized; Prop-non-vacuity — the wrong criterion (proven); the corrected front = a data-anchor with a spectral invariant
import EuclidsPath.Engine.MersenneBranch -- Mersenne: embedding 2^p−1 = 6m_p+1 + twin-criterion; HONESTLY: Mersenne-twins ⟹ twins (NOT the other way around!)
import EuclidsPath.Engine.MersennePaymentConflict -- payment route: sound-ledger + pressure ⟹ ∞ Mersenne-twins ⟹ TwinLowers.Infinite; HONESTY: for the canonical ledger pressure = a consequence
import EuclidsPath.Engine.MersennePeelPressure -- pressure splitting: coverage+payment-law, then debt+realization; chains up to TwinLowers.Infinite; canonical collapse = honesty
-- COLLATZ (grounded on the same first cause; boundary = the FOURTH field of step00FirstCause, §18):
import EuclidsPath.Engine.CollatzEngine     -- Collatz: the strict law is VIOLATED (the odd step raises the height, no Lyapunov); even_marginal/odd_accel_ascends/not_descent_on_odd 🟢
import EuclidsPath.Engine.CollatzFuel       -- Collatz: fuel is finite ON AVERAGE (drift ×0.864), but there is NO monotone height (k=1 at n≡1 mod 4 grows it); no_monotone_height 🟢
import EuclidsPath.Engine.CollatzTugOfWar   -- Collatz: rope vs engine, window_budget ⟹ descent when it prevails; HERO reaches_one_of_countingLaw (per-n, conditional); the universal rope law is REFUTED 🟢 (not_ropeCountingLaw_27, ropeLaw_universal_refuted — witness n=27); refuting the conjecture = a perpetual engine 🟢
import EuclidsPath.Engine.CollatzFirstCause -- 🟢 POST-MORTEM of the decree: the fourth boundary WAS TAKEN AND REMOVED (the tripwire fired — the law is false, the decree overpaid into falsehood); the trilemma closed by a forged refutation (as with YM); epistemics survived (collatzCause_unknowable, collatz_open_status); the Collatz conjecture 🔴 open
import EuclidsPath.Engine.LocalPNPNode -- strict local P/NP-node + a concrete instantiation (at A≤4 incompressibility is UNCONDITIONAL); scope guard: NOT the classical P/NP
import EuclidsPath.Engine.ClassicalPNPBridge -- bridge to the classical P/NP: the carrier field = extraction of a local resolver from a P-decider; HONESTY: the frame is abstract (trivialFrame separates for free)
import EuclidsPath.Engine.CanonicalSelfReduction -- fuel-protocol self-reduction: run/run_done closed GENERICALLY (termination proven); FaithfulPFrame cuts off trivialFrame
import EuclidsPath.Engine.RiemannLayerBoxFront -- layerbox-series (25): residue-tables/move-algebra/blocker-cores; HONESTLY: mod-6 arithmetic + generic assemblies are proven, the rest — scaffolds
import EuclidsPath.Engine.RiemannTerminalRankFront -- terminal/rank-flow (18); HONESTLY: target_of_noZeros — a consequence as an INPUT; inert firewalls are flagged
import EuclidsPath.Engine.MersenneForwardFront -- Mersenne-forward (34); ATTENTION: the late noEngine-packages are UNINHABITED (a free witness) — the headline-theorems are vacuous, disclosed
import EuclidsPath.Engine.ClassicalFrontierRoutes -- P/NP-front (42); the false falseDecider removed; ~30 slogans = True; input-repackagings are flagged
import EuclidsPath.Engine.RankClosureFront -- rank-closure (6); the separation is entirely conditional on an unbuilt witness
import EuclidsPath.Engine.FiniteKnowledgeBarrier -- STRICTLY: a finite system knows almost nothing about twins (purity of classes, propext-only); the two walls — one nature
import EuclidsPath.Engine.RiemannDualEngineFront -- dual route (3): synchronization of payments phantom/genuine, meeting⟺rank change, counting bridge twins↔meetings↔zeros; HONESTLY: all laws — field-inputs, the bridge counts NON-trivial (not off-critical) zeros
import EuclidsPath.Engine.RiemannManifestationFront -- GREEN Riemann chain of the first cause: the manifestation law of deviations + impossibility at resolved scales (L2) + essence-lemma (L3) + audits (law⟺RH UNDER the boundary, L7)
import EuclidsPath.Engine.RiemannLawEpistemic -- 🟢 epistemics of the manifestation law: self-grounding self-destructs (riemannCause_unknowable); a deviation with the books reconciled builds an engine (deviation_carries_engine_at_resolved_scale); the boundary is alive — epistemics about the impossibility of INTERNAL grounding
import EuclidsPath.Engine.RiemannSpectralAnchor -- Hilbert–Pólya anchor in two layers: 🟢 SpectralRealisation ⟹ RH (a verbatim bridge to mathlib-RH) + AUDIT: realization ⟺ RH for free (spectralRealisation_iff — the renaming is disclosed) ⟹ the substantive layer must carry an OPERATOR — 🔴 OperatorSpectralAnchor (self-adjointness+spectrum, mathlib-structures); taint does not grow
import EuclidsPath.Engine.PNPRankPaymentFront -- GREEN P/NP-chain: NP = full payment of rank certificates, P = rank-fast passage; SEPARATION AT A ≤ 4 UNCONDITIONAL; the trilemma of candidates for the third decree field proven (two refutable, the third vacuous); VACUITY No. 4: decider-gated extraction-fronts are classically empty
import EuclidsPath.Engine.PNPFirstCause -- 🟢 EPISTEMIC COMPLEMENT of P/NP (mirror of CollatzFirstCause): the P/NP solution is unknowable FROM WITHIN a finite-fuel strictly-correct machine (pnpCause_unknowable); paying an unbounded supply from within = a perpetual engine (no_internalisedPNPGround, reuse of no_fullPayment_of_unboundedSupply + no_perpetual_engine_on_nat); WITHOUT a decree (P/NP has no honest boundary — trilemma); NOT the classical P≠NP, NOT Gödel; taint does not grow
import EuclidsPath.Engine.PNPDataAnchor -- 🔴 data-anchor of the machine model: TM2CompositionLaw exactly where mathlib proof_wanted comp; TM2FrameBridge with a FIXED encoding (the ∃-form is machine-exposed as a renaming of True — tm2DecidesWithSomeEncoding_free); 🟢 the model is inhabited (id); anchors_do_not_decide_pnp; taint does not grow
import EuclidsPath.Engine.YangMillsFront -- GREEN Yang–Mills branch: gapless-spectrum = a perpetual engine (halving-ladder = ℝ-warning of the cascade); the quantization law ⟹ the mass-gap (EPMI-killer); trilemma of the fourth boundary: universal refutable, existential vacuous, manifestation incompatible with the boundary; per-model law ⟺ gap GREEN — there is NO fourth field; Clay not declared
import EuclidsPath.Engine.YangMillsEpistemic -- 🟢 epistemics of YM: the ladder = a genuine engine (ladder_carries_real_engine, quantizedLadder_carries_perpetual_engine); ymCause_unknowable; the L9-collapse disclosed honestly
import EuclidsPath.Engine.NavierStokesFront -- GREEN NS-branch of smoothness: a singular cascade = a perpetual engine (killed by the budget under energy balance); the energy identity and the integral form of the solution BY TAKING THE INTEGRAL (FTC, E3-valued); trilemma of the fifth boundary: universal refutable (cookedFlow), existential vacuous (zero), manifestation incompatible with the boundary; surrogate ≠ C^∞ — disclosed; Clay not declared
import EuclidsPath.Engine.NavierStokesR3Assembly -- TRANSITION TO ℝ³: the NS integral assembled on a box — R1 the energy derivative under the integral (dominance), R2 the divergence theorem REALLY employed (faces vanish by support), the energy balance DERIVED green for the box-class ⟹ cascade smoothness WITHOUT a decree; pressure gated for the first time (the Forging-B channel closed); the box→ℝ³ limit is absent in mathlib — an external gap; the three E3-killers — named inputs
import EuclidsPath.Engine.NavierStokesClayReduction -- REDUCTION TO A SINGLE THEOREM: the exact Clay-(A) statement encoded; a green logical reduction "known theory (Kato+BKM) + GlobalVorticityControl ⟹ Clay-(A)"; the open CORE isolated and named (vorticity control, supercriticality barrier); NS is NOT solved — an honest map of tiers, taint unchanged
import EuclidsPath.Engine.CascadeBudget -- T2: an abstract budget-lemma (a finite budget cannot withstand eternal UNIFORM dissipation) + a CONCRETE finite shell-model (a real ODE) + the honest boundary budget_misses_nonuniform (a non-uniform cascade slips away); the first formalization of a fluid shell-model
import EuclidsPath.Engine.DyadicBlowup -- T1: a rigorous core of super-linear blowup (y'≥Cy² ⟹ finite time) + the Katz–Pavlović model (energy telescope) → dyadic_blowup; A→B: the drive DERIVED from the raw λⁿ-relations — for the exact self-similar solution (§2bis: ssMode, ssLead_drive) and for the CLASS via the front invariant (§2ter: FrontDomination, frontDrive_of_invariant), the monolithic superlinearDrive-hypothesis narrowed; infinite-mode conservation (the front) is honestly open; §2quater: the source n=0 is unknowable from within (🟢); the cascade = a REALIZED engine
import EuclidsPath.Engine.CollapseExtinction -- DUAL of DyadicBlowup: finite-time EXTINCTION for a sublinear inward drive r' ≤ -C·r^α (0≤α<1) → collapse to r=0; the abstract ODE core shared by turbulent blow-up and gravitational collapse; self-contained (no first-cause layer); non-vacuity witness (α=0) included; NOT physics — the collapse/supernova/Big-Bang reading is metaphor
import EuclidsPath.Engine.ContinuousEngine -- CONTINUOUS-TIME engine H(t)=r^t over ℝ (M1, the first formal one) — ℝ-analogue of the falsity of EPMI on the continuum; convergence≠contradiction (M2), supertask scale→0 up to T (M3), the Clay linkage as Iff.rfl (M4), an honest endpoint (M5); M6 — WHERE the engine is IMPOSSIBLE: uniform drain on finite fuel forces the endpoint (T≤H0/β), r^t survives only by non-uniformity (dividing_line); NOT a solution of NS, the Tao barrier, taint unchanged
import EuclidsPath.Engine.DyadicFirstCause -- 🟡 the source of the cascade = the first cause (manifestation): the dyadic blowup attaches to the masks through its source; the green derivation of the drive (STEP A/B) does NOT depend on this; taint grows
import EuclidsPath.Engine.CollapseFirstCause -- 🟡 DUAL yellow layer: the collapse REBOUND (fresh 0→1 out of r=0) is decreed by the first cause via REUSE of nsBoundary (no new boundary); green wall collapse_drive_forbids_rebound is axiom-free; extinction math does NOT depend on this; taint grows by two
import EuclidsPath.Engine.HawkingLeakyHorizon -- 🟢 a Hawking-style leak relocates the barrier by ZERO: outside reconstruction is already permitted, inside derivation still impossible (hawking_leaky_horizon = ⟨beyondHorizon→twins, ¬internalised⟩); corroborates no_internalisedHorizonBoundary; NOT physics, no new boundary, taint unchanged
import EuclidsPath.Engine.DyadicFrontWindow -- 🟢 T-relative window of the front invariant: FrontPreservedUntil + the bridge "forever ⟺ up to any T" (pure quantifier logic); inhabitedness of ssMode on the whole [0,T) with a FIXED m₀=β_{J+1}/T (monotonicity of g) and an inhabited gap with Forever; the finite-window MVT-step §2ter closed (mathlib segment-estimate; gap velocity bounds = a named local input) + a stationary witness; the red input FrontPreservedForever is NOT closed, taint does not grow
import EuclidsPath.Engine.UniversalEngine -- UNIVERSAL TRANSFER of the engine: PerpetualEngine over ANY relation; impossibility from an INTERNAL cause (definition = infinite descent ⟂ WellFounded), perpetualEngine_iff_not_wellFounded; transfer by rank (no_perpetual_engine_of_rank/natRank — all fronts are consequences of a SINGLE theorem; EPMI shown as an instance); CONTROLLER — over ℝ the engine WORKS (perpetualEngine_on_real, reuse), universal_engine_dividing_line (M6); the core — mathlib-well-foundedness, the contribution is formalization-unifying; green, taint unchanged
import EuclidsPath.Engine.BSDFront -- THE EIGHTH MASK: the shadow of Birch–Swinnerton-Dyer. The engine's role is DIFFERENT — it is itself the method of DESCENT proving the algebraic rank: bsd_descent_has_no_engine (descent by height on the REAL points W.toAffine.Point has no engine = Mordell–Weil termination, reuse of UniversalEngine; references to the real Height/Northcott/fg_of_descent/LSeries); parity-bridge bsd_parity_is_rankParity to Liouville (rank-parity node). 🔴 honestly open: AnalyticRank (order of the zero of the REAL WeierstrassCurve.LSeries at s=1), WeakBSD (rank=ord L) — analysis outside mathlib, the engine does NOT close it. Trilemma: there is NO honest boundary (the parity decree is vacuous over a stub of the root number) — fallback 🔴, the axiom untouched, taint does not grow. NOT a proof of BSD
import EuclidsPath.Engine.BSDEpistemic -- 🟢 epistemics of BSD: THE DEGENERACY IS FIXED MACHINE-WISE (bsdGround_coincides_with_engine — BSD has no epistemic axis of its own, its linkage coincides with the ban on the descent engine, and this is a theorem); the AnalyticRank gate is freely satisfiable (an honesty lemma); taint does not grow
import EuclidsPath.Engine.BSDAnalyticAnchor -- 🔴 data-anchor of the analytic rank: ContinuedLFunction (Λ + reconciliation with the LSeriesSummable-domain + AnalyticAt at 1 — mathlib does NOT provide the continuation) ⟹ 🟢 analyticRankOf := analyticOrderAt Λ 1 (the genuine order of the zero) + WeakBSDContentful; the contrast with the free old gate disclosed; taint does not grow
import EuclidsPath.Engine.ChowlaFront -- NEIGHBOUR (chapter 54): the shadow of Chowla/Sarnak — a rank-parity node. 🟢 the real Liouville λ=(−1)^Ω: λ²=1, the correlation diagonal = x (perfect self-correlation of parity), the parity flip (reuse of RiemannLiouville), liouvilleSum_eq_L; "deviation = collapse of parity into a single parity". 🔴 ChowlaConjecture (correlations o(x)), SarnakConjecture — open (Tao: only the log/odd case). NOT a proof; taint does not grow
import EuclidsPath.Engine.ChowlaEpistemic -- 🟢 epistemics of Chowla (P/NP-type, no decree): collapse of parity = tail-constancy of λ — self-destructs against the flip-wall (no_parityCollapse_of_flip CONSUMES the flip-supply, CORR); the correlation ≡ x (mod 2) — zero is unreachable on odd slices; λ(2^k)=(−1)^k — both signs cofinally; chowlaCause_unknowable; summary chowla_locked_behind_flip_wall (Chowla has no engine-fact — honestly a flip-wall); NOT a solution of Chowla/Sarnak, NOT Gödel; taint does not grow
import EuclidsPath.Engine.AbcFront -- NEIGHBOUR (chapter 54): the shadow of abc. 🟢 the REAL Polynomial.abc (Mason–Stothers PROVEN) — polynomial abc = a power bound via the radical = "no infinite escape" (finiteness); the real Nat/Int.radical. 🔴 AbcConjecture (integral) is open (Mochizuki's claim is not accepted by the community). NOT a proof; taint does not grow
import EuclidsPath.Engine.AbcEpistemic -- 🟢 epistemics of abc: the radical toolkit (natRad_pow/mul); the naive law ε=0 is REFUTED (1+8=9, rad=6<9) + an unbounded supply of quality (the LTE-family 3^(2^k)); linkage on LinearAbcLaw with pigeonhole; abcCause_unknowable — honestly about the NAIVE form, the real abc-gate is untouched; taint does not grow
import EuclidsPath.Engine.BealFront -- NEIGHBOUR (chapter 54): the shadow of Beal/Fermat–Catalan — pure Fermat-descent = engine. 🟢 the REAL Polynomial.flt_catalan (PROVEN, descent over char p) + fermatLastTheoremFour/Three (descent) + descent-without-engine (reuse of EPMI/UniversalEngine). 🔴 BealConjecture, FermatCatalanConjecture (canonical form: triples of VALUES) open (Darmon–Granville: only fixed exponents); the tuple form of the gate is GREEN-REFUTED (fermatCatalan_tupleForm_refuted — a formalization lesson). NOT a proof; taint does not grow
import EuclidsPath.Engine.LehmerFront -- NEIGHBOUR (chapter 54): the shadow of Lehmer/Mahler measure — height-finiteness as in BSD. 🟢 the REAL finite_mahlerMeasure_le (Northcott: bounded degree+measure ⟹ finite = no infinite height descent) + pow_eq_one_of_mahlerMeasure_eq_one (Kronecker: measure 1 ⟺ roots of unity) + descent-without-engine. 🔴 LehmerConjecture (the sharp constant c>1) is open. NOT a proof; taint does not grow
import EuclidsPath.Engine.LehmerEpistemic -- 🟢 epistemics of Lehmer (P/NP-type): the Northcott horizon is finite green (mahlerBox), internal payment of an infinite family = a pigeonhole-engine; lehmerCause_unknowable
import EuclidsPath.Engine.LehmerBoundedDegree -- 🟢 Lehmer AT BOUNDED DEGREE is proven (lehmer_at_bounded_degree: for each n there is c>1 with no measures in (1,c) — a Northcott-minimum of the window); 🔴 UniformLehmerGap (a single c for all n) = the conjecture itself, the iff-audit disclosed; taint does not grow
import EuclidsPath.Engine.LandauFront -- NEIGHBOUR (chapter 54): the shadow of Landau n²+1 (the zoo, mirror of SideInfinitude). 🟢 the bridge unboundedness⟹infinitude + the fact oddLandauPrime_even_k (odd k ⟹ k²+1 even ⟹ the only prime = 2). 🔴 Landau4thUnbounded (Hardy–Littlewood) is open (Iwaniec: ≤2 factors). NOT a proof; taint does not grow
import EuclidsPath.Engine.LandauEpistemic -- 🟢 epistemics of Landau: canonization of the gate (iff-infinitude, the even channel via max N 1), the bridge to Dirichlet 1 mod 4 (the real Nat.forall_exists_prime_gt_and_modEq + the divisor wall landauFactor_mod_four), the manifestation law is NOT decreed (the Polignac-class landauRefutation_carries_engine), landauCause_unknowable; the ground/beyondOwnHorizon pair is tautological — disclosed machine-wise; taint does not grow
import EuclidsPath.Engine.HodgeFront -- GREEN Hodge branch: quantized charge = ℕ-height; the engine (descent chain) is dead UNCONDITIONALLY (EPMI); the descent law ⟹ the model conjecture (strong induction on height); collapse law ⟺ conjecture green — there is NO sixth field; trilemma: V1 refutable, V2 vacuous, the chain-form degenerated into a green one, V3 incompatible with the boundary; mathlib does not contain Hodge — Clay not declared
import EuclidsPath.Engine.HodgeEpistemic -- 🟢 epistemics of Hodge: per-model DescentLaw (universal FORGED — hodgeUniversal_forged_refutation); an unpaid class = a chain-engine; hodgeCause_unknowable
import EuclidsPath.Engine.MersenneManifestationFront -- GREEN Mersenne-manifestation: absence witness (Π, ≥ 29), a gated law, M3⁺ "a refutation presents an engine"; the chain 4c+1 does NOT peel (no forging); the trilemma passed, BUT the field is deferred — the heuristic sign is AGAINST (the §16-comment of the quarantine)
import EuclidsPath.Engine.FermatMersenneEpistemic -- 🟢 epistemics of Fermat+Mersenne: both bets deferred (§16/§17) AND there are no internal paths — a refutation under the law/books = an engine; fermatCause_unknowable, mersenneCause_unknowable
import EuclidsPath.Engine.SideInfinitude              -- Dirichlet: the sides 6m±1 are each separately infinite (🟢); reconciling the sides in one centre = the whole open essence
import EuclidsPath.Engine.PolignacBranch              -- Polignac in the grid: cousins (p,p+4) and sexy (p,p+6) on neighbouring centres; classification, instances, bridges (🟢)
import EuclidsPath.Engine.SophieGermainBranch         -- Sophie Germain: the minus side, doubling the centre m→2m; PEARL: SG-primes at p≡3 (mod 4) kill Mersenne (🟢, 23∣M₁₁)
import EuclidsPath.Engine.FermatBranch                -- Fermat branch: F_k ≡ 5 (mod 6) — the minus side; quadratic chain c'+4c=6c²+1; twin-instances k=1,2,4 (up to 65537/65539)
import EuclidsPath.Engine.FermatMersenneLadder -- 🟢 Fermat↔Mersenne ladder: 7∣F_k+2 (odd k — half the indices drop out), 5∣2^p−3 at p≡3 (mod 4) — the §16 heuristic is now green; the bridge mersenne(2^n)=∏F_k + Goldbach uniqueness of the layer; NOT a solution of the infinitudes, taint does not grow
import EuclidsPath.Engine.PerfectNumberBranch         -- Perfect numbers: Euclid–Euler BOTH directions green (Archive); Mersenne primes ⟺ even perfect numbers unbounded (🟢-iff)
import EuclidsPath.Engine.PolignacManifestationFront    -- Polignac-fronts (cousins p+4, sexy p+6): absence witness Π, law, essence M3; there is NO field (§17, sign FOR)
import EuclidsPath.Engine.PolignacEpistemic -- 🟢 epistemics of Polignac: cousinCause_unknowable/sexyCause_unknowable; a refutation with the books reconciled = an engine (reuse of *Refutation_carries_engine)
import EuclidsPath.Engine.BoundedGapsAnchor -- 🔴 anchor of bounded gaps BoundedGaps B (Zhang 70M / Maynard–Tao 246 — NOT formalized, names only) + 🟢 bridges: monotonicity upward in B, the MAIN AUDIT boundedGaps_two_iff_twins (B=2 ⟺ TwinLowers.Infinite — the goal renaming disclosed by an iff), disjunctions of thresholds 4/6 on the Polignac grid; descent 246→2 is NOT asserted (NoDescentFrom246To2Claimed); taint does not grow
import EuclidsPath.Engine.SophieGermainManifestationFront -- Sophie Germain front + anti-Mersenne restriction: SG-manifestation feeds the composite side of Mersenne ("two bets")
import EuclidsPath.Engine.SophieGermainEpistemic -- 🟢 epistemics of Sophie Germain: sgCause_unknowable (+3 mod 4); a refutation = an engine (sgRefutation_carries_engine); the pearl 23∣M₁₁ is unconditional
import EuclidsPath.Engine.GoldbachManifestationFront    -- Goldbach-front (object-witness, decide-decidable): an unpaid even number manifests an engine
import EuclidsPath.Engine.LegendreDesertFront           -- Prime deserts: GREEN Bertrand (a desert does not double) + Legendre-front (the interval is shorter — Bertrand does not decide it)
import EuclidsPath.Engine.GoldbachLegendreEpistemic -- 🟢 epistemics of Goldbach+Legendre: a violation with the books reconciled = an engine; goldbachCause_unknowable/legendreCause_unknowable; the witness is decide-decidable — "verification, not derivation"
import EuclidsPath.Engine.GoldbachLegendreEpistemic     -- 🟢 EPISTEMIC COMPLEMENT of Goldbach+Legendre (mirror of PNPFirstCause): goldbachCause_unknowable / legendreCause_unknowable; self-grounding = an engine-object (builds_engine, paid by no_deviationFlowSupply_at_resolved_scale, NOT P∧¬P); summary goldbachLegendre_locked_behind_engine_status WITHOUT a decree-conjunct (§17); NOT a solution of problems 1742/1808, NOT Gödel; taint does not grow
import EuclidsPath.Engine.OddPerfectManifestationFront  -- Odd perfect numbers (object-witness): the even side is green-constructible, the deviation lives on the odd one
import EuclidsPath.Engine.OddPerfectThreePrimes -- 🟢 an odd perfect number has ≥ 3 distinct prime divisors (classic via abundance: σ(n)/n < p/(p−1)·q/(q−1) ≤ 15/8 < 2); steps: not a prime power, not two primes; taint does not grow
import EuclidsPath.Engine.OddPerfectEulerForm -- 🟢 FLAGSHIP: Euler's theorem — an odd perfect number n = q^α·m², q prime, q ≡ α ≡ 1 (mod 4), q ∤ m; steps: the unique odd exponent, residues of q and α, packing the square; taint does not grow
import EuclidsPath.Engine.OddPerfectEpistemic -- 🟢 epistemics of odd perfect numbers: presenting a witness = presenting an engine (conditional on the law); oddPerfectCause_unknowable; variant (b) — non-degenerate
import EuclidsPath.Engine.FermatManifestationFront      -- Fermat-front: localization ≥ 65537; the sign is INVERTED more strongly than Mersenne (only F₀–F₄) — §16 verbatim

-- ⚠️ QUARANTINE: the SINGLE axiom of the repository — the first cause with TWO boundaries:
-- the twin-node (causalBoundary) and the Riemann manifestation law (riemannBoundary, §10).
-- Everything depending on it — CONDITIONALLY, machine-flagged by the verifier as AXIOM-TAINTED.
-- twin_prime_conjecture is NOT closed through this module and remains sorry;
-- riemannHypothesis_from_firstCause — a reduction closed by decree, NOT a proof of RH.
import EuclidsPath.Engine.CausalClosureAxiom
import EuclidsPath.Engine.GeometryFront -- GEOMETRY OF THE PATH: the arrow of time (lexRank ↓), curvature κ=1−outdeg COMPUTED (spectrum −8…+1, χ(cone3)=−5), flatness everywhere ⟹ an engine; lines are finite — Euclid's SECOND postulate falls, there are no parallels; a web through the grave of zero; the intersection of lines 🟡 from the same first cause, but it cannot be known from within 🟢 (exactly 2 tainted-declarations)
import EuclidsPath.Engine.WellFoundednessCanon -- 🟢 WELL-FOUNDEDNESS CANON: the engine reaches the classical termination canon — Hydra (mathlib WellFounded.cutExpand), the ω-worm (first TRANSFINITE ordinal rank fed to no_perpetual_engine_of_rank — the Goodstein/hydra mechanism: value explodes, ordinal drops), Markov/Vieta descent (green termination wall + honest red Frobenius-uniqueness gate); taint unchanged
import EuclidsPath.Engine.PoincareRecurrence -- 🟢 the reversibility dividing line: a measure-preserving (reversible) map recurs by Poincaré (mathlib Conservative) vs a strict descent on a well-founded carrier never returns; mirrors universal_engine_dividing_line; measure-theory imports isolated here
import EuclidsPath.Engine.ReciprocalDividingLine -- 🟢 SHARPENING universal_engine_dividing_line: the ℝ-engine descends through the GAPS, not the ℕ-skeleton (strictAnti_meets_nat_finitely) — why the ℝ-side cannot be pressed for arithmetic; plus twinCenters_accumulate_at_zero_iff_infinite (0 a limit point of {1/m twin} ⟺ twins infinite — an honest RELABELLING, axiom-clean, NOT progress on the conjecture)
import EuclidsPath.Engine.Step00SerialTwinBoundary -- 🟢 the twin boundary in SERIALITY form: SerialTwinBoundaryObligation ("live time cannot stall inside the twin boundary without a witness") ⟺ UnboundedTwinCenters ⟺ TimePumpStep00Obligation; the axiom now assumes EXACTLY the twin conjecture, transparently (a reduction, NOT a proof)
import EuclidsPath.Engine.Step00MinimalReachableRankPatch -- 🟢 constructive rank-exhaustion (Reachable + strong induction); timeSerialRankEngine_reaches_close_from_live is AXIOM-FREE; honest companion, conditional (no progress on the open node)
import EuclidsPath.Engine.Step00BackwardTimeMachinePatch -- 🟢 "time forced to keep running backward = impossible perpetual engine"; BackwardTimeMachine.impossible axiom-free + explicit budget lemma making the finite ℕ-ceiling the wall; honest companion, conditional
import EuclidsPath.Engine.Step00PaidCausalStepper -- 🟢 "pay the present to know the future": a LOCAL paid causal stepper (twin-witness ∨ violation ∨ paid step with fuel↓); forces_close_or_violation/forces_twin axiom-free; paidStepper_iff_unboundedTwinCenters is the WALL machine-checked (obligation ⟺ exactly the twin conjecture) — honest localization, NOT a proof
import EuclidsPath.Engine.Step00NoFutureOracle -- 🟢 HONESTY AUDIT (no future oracle): FutureOracle = the goal (nonempty_futureOracle_iff_twinAbove: Nonempty (FutureOracle M0) ⟺ ∃ twin > M0); the reverse ⟸ half of every "X ⟺ twins" is an ORACLE construction (oracleStepper, powered by the future twin) — the circular half. A causal (oracle-free, arithmetic) stepper is the genuine open node; this file certifies where the circularity lives so it cannot pass as a causal proof
import EuclidsPath.Engine.Step00PaidObservation -- 🟢 HONESTY AUDIT (paid observation): two guardrails, both axiom-clean. (A) causality by indistinguishable worlds — not_causal_of_usesFuture: a decision distinguishing two locally-identical worlds is NOT causal (it uses a future oracle); AXIOM-FREE. (B) paid observer — horizon_bounded/cost_bounded: a finite resource bounds total observation & reachable horizon (no free global observation). FreeGlobalObservation ⟺ the twin conjecture: the "free" global fact may be an OUTPUT, never an INPUT to a local step
import EuclidsPath.Engine.Step00CompletePatch -- 🟢 POST-MORTEM: not_theStrictLastStep00Obligation refutes the OLD finite-key twin boundary (Dirichlet adic construction) — the reason the repair was needed; the axiom no longer derives False
import EuclidsPath.Engine.Step00FrontClosureAudit -- 🟢 FRONT-CLOSURE AUDIT: why ONLY twins closes the boundary transparently. twin_front_closes (two-sided ⟺, non-vacuous) = the template met; rh_front_closes_only_under_twin_boundary (RH ⟺ manifestation law ONLY under twin-strength hypothesis) = conditional; ns_manifestation_incompatible_with_twin_boundary (NS manifestation form + twin boundary ⟹ False) = cannot be bundled. Machine reason RH/NS were withdrawn from the decree; the deeper asymmetry (vacuous RH reverse, cosmetic bridge, NS reverse blocked by missing ℝ³ divergence theorem) is documented in prose, not a theorem
import EuclidsPath.Engine.Step00GenealogicalOrnament -- 🟢 SECOND ROUTE (curvature/ornament), green scaffold: safeHole_implies_twin (real sieve lemma, Nat.prime_def_le_sqrt) + twinBound_forces_noSafeHole + no_perpetualEngine (axiom-FREE rank exhaustion). The route reaches TwinLowers.Infinite ONLY via OrnamentEngineBridge (NoSafeHoleAbove ⟹ perpetual engine) — a NAMED hypothesis, NOT an axiom: proving it IS the twins (the sieve parity barrier). Relocates the single open node from the near-tautological seriality boundary to a concrete curvature/sieve statement; does NOT close the twin sorry
import EuclidsPath.Engine.Step00OrnamentCurvatureReservoir -- 🟢 QUANTITATIVE DECOMPOSITION of the wall (consolidates the OrnamentBridge_* patch pipeline): CurvatureReservoir (total=safe+boundary+dissipation+cycle) + positiveCycleResidue_of_gap (green ℕ arithmetic) + MarginCapEstimates (finite cap/margin form) turn the black-box OrnamentEngineBridge into a concrete finite inequality boundaryCap+dissipationCap+margin ≤ totalLower. κ=-5 sign-normalisation (usableSurplus, candidate margin 5) with the honest audit minusFive_requires_cap_domination (5 alone does NOT settle it). TailFromFive (twins at 1,2,3,5,7; not 4,6 — start at M0≥5). twinLowersInfinite_of_curvatureFamilyFromFive reaches the goal CONDITIONAL on the margin family (the parity barrier); no new axiom, twin sorry untouched
import EuclidsPath.Engine.Step00RelativeOrnamentCurvatureCorrections -- 🟢 RELATIVE-CURVATURE scaffold (generic): RelativeCurvatureCertificate carries relIndex plus noSafeHole_relIndex_zero (collapse) + relIndex_computed : relIndex = -5 (the NAMED hard field). not_noSafeHole_of_relativeCurvature (0=-5 ⊥) → safe hole → twin, all CONDITIONAL on the certificate; tail-from-five family. relIndex=-5 is a certificate shape (intended: relative Euler index after quotienting the killed boundary — the quotient is NOT constructed), NOT proved, NOT gaussBonnet_cone3
import EuclidsPath.Engine.Step00RelativeCurvatureInstance -- 🟢 THIN adapter: instantiates the generic scaffold with the REAL ActiveSieveSafe / TwinCenterZ (via safeHole_implies_twin, twinCenterZ_five, infinite_of_unbounded_centers); no new structure/axiom, GeometryFront/gaussBonnet_cone3 NOT imported. twinLowersInfinite_of_realRelativeCertificates reaches TwinLowers.Infinite CONDITIONAL ONLY on certs : ∀ M0≥5, RelativeCurvatureCertificate ActiveSieveSafe M0 (the relIndex=-5 family = parity barrier); everything else discharged by real green lemmas. twin sorry untouched
import EuclidsPath.Engine.Step00OrderedExponentGeometry -- 🟢 ORDERED EXPONENT GEOMETRY (graded layer over Nat.factorization): ordered exponent vectors, Ω/ω, powerDefect = Ω−ω with powerDefect_eq_zero_iff_squarefree, gapDefect, ordered prime blocks B(i,k) (squarefree, Ω=ω=k, defects 0, contiguous index support, sliding window), Ω-preserving exchange (blind to the parity sign, visible to the grades — the barrier mechanism, machine-form); NEW GREEN BRIDGE activeSieveSafe_iff_twinCenterZ: the safe hole is POINTWISE the twin predicate, both directions — all route content is cofinal existence. No new axiom, twin sorry untouched
import EuclidsPath.Engine.Step00TwoEngineOscillation -- 🟢 TWO-ENGINE OSCILLATION: collision laws of the wing pair (no clock strikes both wings — Carrier; wings coprime, prime supports disjoint = fermionic exclusion), q-periodic strikes on antipodal phases ±(6:ZMod q)⁻¹ with exactly q−2 admissible classes (the local Hardy–Littlewood factor), sign walk never zero, all four single-wing sign corners visited infinitely often ELEMENTARILY (powers of 5 and 7 mod 6, no Dirichlet); twin_corner_grading displays the honesty ladder (joint corner = Chowla-adjacent, NOT stated; its graded substate = the twin node). twin sorry untouched
import EuclidsPath.Engine.Step00GradedKilledBoundary -- 🟢 GRADED KILLED-BOUNDARY ROUTE: named hypothesis structures GradedKilledBoundaryMatching (discrete-Morse shaped, fields 1:1 with Finset.sum_involution) + SignedBlockProductModulusEstimate; GREEN killed_sum_zero + survivor_exists + the genuine bridge building RelativeCurvatureRealizationProblem certificates (relIndex := -5) and reaching TwinLowers.Infinite conditional ONLY on the two named inputs; toy witness (nonempty killed sector), anti-cheat interlocks (trivial matching free, estimate cannot ride it); DISCLOSURE iff: route ⟺ unbounded twin centers (the parity barrier in graded matching + signed-window form, not a shortcut). twin sorry untouched
import EuclidsPath.Engine.Step00GradedWindowComplex -- 🟢 GRADED WINDOW COMPLEX: sector split of the ornament window (survivor cells = clean centers vs killed cells), eulerChar_sector_split (χ = relIndexStruct + killedEuler); THE HORIZON-INVISIBILITY THEOREM relIndexStruct_m0_invariant (census law L15: all M0-dependence lives in the killed sector — the true, provable content behind the numerically REFUTED constant −5 decree, ledger L13); survivor tournament eSS_eq_choose_two (census law L14: E_SS = C(V_S,2) exactly); kernel calibration relIndexStruct 5 M0 cone3 = −7 at EVERY horizon. twin sorry untouched
import EuclidsPath.Engine.Step00OrnamentTwoComplex -- 🟢 ORNAMENT 2-COMPLEX: Face (directed 2-simplices + killed squares), computed triFaces/quadFaces; KEYSTONE no_absorber_in_tri ⟹ triFaces_m0_invariant (triangle layer horizon-free); quadFaces_mono_m0 (ALL horizon-dependence of the 2-skeleton = killed squares, monotone); three-term split χ₂ = relIndexStruct2 + killedEuler2 + #quads with relIndexStruct2_m0_invariant (census law L15 in dimension 2); tournament law card_triAllSurv_eq_choose_three (= C(V_S,3)); kernel calibration cone3: 4 tri + 6 quads, χ₂ = 5, relIndexStruct2 = −3 at EVERY horizon. twin sorry untouched
import EuclidsPath.Engine.Step00RankCertificate -- 🟢 RANK CERTIFICATES (det-free, decide-checkable): IntRankCert (factorization bounds rank above, pseudo-inverse pair bounds below, all fields decidable ℤ-data) + rank_eq — ONE integer certificate pins Matrix.rank over EVERY field (mathlib rank is noncomputable, minors explode — both bypassed); finrank_homologySpace (dim H = e − r1 − r2, fields only — ℤ-torsion disclosed blind spot) + betti_euler glue + demo instance. The engine for gain-complex Betti certificates. twin sorry untouched
import EuclidsPath.Engine.Step00TwinJacobsthalWall -- 🟢 TWIN-JACOBSTHAL WALL (Line C of the assault): CleanGapBound/PeriodCert with the periodicity lift; KERNEL-PROVED gap certificates at the exact measured gaps G(5)=2, G(7)=5, G(11)=7, G(13)=11 (pure decide over full periods up to 5005 — no native_decide, no new axiom); the named wall TwinJacobsthalBound(Cofinal) = gap ≤ A²/7 at (cofinally many) prime scales — exact-verified through A=23, margins 0.35–0.61, plausibly STRONGER than twins (Cramér-strength), never an axiom; GREEN escalation twinCenters_unbounded_of_twinJacobsthalCofinal (infinitude of primes only, sink_is_twin finisher) reaching TwinLowers.Infinite through the graded route. twin sorry untouched
import EuclidsPath.Engine.Step00GainLayer -- 🟢 GAIN LAYER: wingGain = graded distance from the twin state (wingGain_eq_zero_iff, both ways); COUNTING BRIDGE balanced_window_has_twin (gain-balance certifies twins — the correct polarity); tFill_relative_euler = chi-form of the G1-confirmed T-fill law (tournament noise cancels the cone fill by Pascal; exact on all 36 census regions; canonical-fill contractibility REFUTED by the gate — ledger L21). G3 verdict recorded: window-level gain invariants PARITY-BLIND-UNDER-ESCALATION (L22). twin sorry untouched
import EuclidsPath.Engine.Step00ExtremalGapGeometry -- 🟢 EXTREMAL GAP GEOMETRY: WitnessChain certificates (a SUBSET of clean residues with gaps ≤ L majorizes the true gaps — completeness not needed, ~30x cheaper than full period certs); chain_covers + cleanGapBound_of_witnessChain lift; KERNEL instance A=17 (2193 witnesses, pure decide, 69s) → twinJacobsthal_at_17 with EXACT margin 17²/7 = 41 = G(17); clean_reflect (the clean set is mirror-symmetric within a period — wings swap); A=19 attempted and honestly dropped (kernel memory wall, future routes documented). twin sorry untouched
import EuclidsPath.Engine.Step00PackingGeometry -- 🟢 PACKING GEOMETRY (bidirectional repacking): Repackable vocabulary with DISCLOSED pointwise equivalences (= composite = Omega ≥ 2); TOTAL REPACKING theorem support_renewal_succ (+1 demolishes every side of the box — consecutive supports disjoint); centered collision form twin_collision (repackable stable center 6m flanked by two unrepackable directional defects — definitional dressing, disclosed); three-phase competition law center_strike + neutral_card = q−3 per clock; truncated-dimension control ladder truncDim (kernel-decidable, P_B-periodic via the clean_shift pattern, bounded by omega) with THE KEYSTONE clean_iff_truncDim_wings_eq_zero welding it to the Clean/PeriodCert/WitnessChain stack; kernel census by pure decide: phi(210)=48, doubly-clean 15 (B=7) and 1485 (B=13, 5005-element decide). twin sorry untouched
import EuclidsPath.Engine.Step00GraveStructure -- 🟢 GRAVE STRUCTURE: descent sinks (Genealogy.IsRoot = twins, root_iff_twin) welded to every later layer — grave_iff_gainZero/collision (DISCLOSED restatements), descent_reaches_grave (the engine terminates at gain-zero sign-plus cells), balanced_window_has_grave, grave_strike_avoidance (graves occupy exactly the q−2 admissible classes of every clock — the phase-law placement of sinks), strike_exit_below (non-artifact per-clock exit), capstone grave_exit_control (assembly of greens, disclosed). twin sorry untouched
import EuclidsPath.Engine.Step00TruncatedSignLadder -- 🟢 TRUNCATED SIGN LADDER (the sign-flipping induction step): cappedVal/truncOmega/truncSign (kernel-friendly, no factorization in the decide path), MANDATORY disclosure truncSign_one_not_lambda (K=1 is multiplicity-blind), periodicity mod prod q^K, THE STEP THEOREM truncOmega_extend + truncSign_step (escalation multiplies by the new clock only), flip loci = the three strike phases (truncSign_flip_left/right/center), law preservation admissible_card_pair = (q1−2)(q2−2) (the L9 goal in phase coordinates), kernel censuses incl. signedCensus_7; S5 disclosure: the full lambda is the LIMIT of the ladder — the wall is exactly the exchange of limit and law, no Lean statement about the limit. twin sorry untouched
import EuclidsPath.Engine.Step00WitnessChainKernel -- 🟢 FUSED KERNEL WITNESS CHECKER: cleanListB/chainB/witnessChainB (pure Nat-primitive Bool folds) + one-directional correctness to WitnessChain; the 16GB elaborator wall DEFEATED (decide +kernel: 11x, Bool fusion: 5x — A=17 recheck 1.3s vs 69s); KERNEL INSTANCE A=19 (33412 witnesses, 212s, 23GB peak documented honestly) → cleanGapBound_19 + twinJacobsthal_at_19 with the SECOND EXACT margin 361/7 = 51 = G(19); twin_gap_below_wall (grave spacing = clean spacing inside the active window) with instances at A=13,17; witnessChainB theorems carry NO axioms at all. twin sorry untouched
import EuclidsPath.Engine.Step00TwinJacobsthalSharp -- 🟢 SHARP (TWO-REGIME) WALL: a SECOND named hypothesis TwinJacobsthalBoundSharp(Cofinal) at constant 1/14 for A ≥ 11 (regime boundary EXACTLY at 11: fails at 5,7 — breakers proven; TIGHT at A=19: ⌊361/14⌋ = 25 = G(19) — falsifiable at the next solver scales); glue theorems to the load-bearing 1/7 wall (which STAYS load-bearing) and to TwinLowers.Infinite; instances at 11/13 now, 17+ awaiting the true-gap phase-cover certificates; margin ledger + regime breakers as exact integer checks. Includes the F1 honesty corrections in ExtremalGapGeometry/WitnessChainKernel docstrings (chain step 41/51 ≠ gap 18/25). twin sorry untouched
import EuclidsPath.Engine.GenealogyBasins -- 🟢 GENEALOGY BASINS (the honest form of reverse induction from infinity): TwinFreeInterval + twinFree_descent_exit (every genealogy entering a twin-free interval passes below its floor — the correct replacement of the literal dichotomy); computable canonicalPeel + grave (WF recursion); KERNEL TOMBSTONE dichotomy_literal_refuted (the pair (8,9): twin-free, graves 1 ≠ 2 — the deterministic reading is FALSE; the relational reading separately vacuous, disclosed); grave_le_of_noNewTwinAbove (the real-forest form of the GlobalOldTwinAbsorption placeholder); grave_infinite_fanin (pigeonhole: no new twins ⟹ some grave has INFINITE basin — pump stays THE wall, one wall three costumes); reverse_fanin_infinite (the Koenig tombstone: the reverse tree branches infinitely at EVERY node, labels = primes — why [Finite Lbl] was counting content); adjacent_wings_pairwise_coprime + adjacent_peel_alphabets_disjoint (infinitely close points differ: adjacent centers have disjoint first-step peel alphabets); escape_iff_unbounded_twins (DISCLOSURE: the escape strategy is EQUIVALENT to the goal). twin sorry untouched
import EuclidsPath.Engine.Step00GraveDepthKernel -- 🟢 GRAVE DEPTH KERNEL: path certificates (stepB/twinB/pathB fused Bool folds, primeB via fuel-driven trial division — minFac is WF-opaque to the kernel, disclosed) + one-directional soundness to ReachTwin; KERNEL CENSUS depthCensus_1_2000 (every m in [1,2000] reaches a twin in ≤ 3 peel steps; 19s, the Bool theorem carries ZERO axioms) → reachTwin_all_1_2000. Census verdicts (unconditioned, ledger L43): depth ≤ 3 REFUTED (max 4 at m=4229 and at 10¹²); BOTH drop-control rules REFUTED (max-target min ratio decays ~m^(-0.53)); M(N) reverse-generation stalls at 4228; disjoint-reach twin-free adjacent pairs EXIST (4053 ≤ 10⁵, first (27,28)). Mandatory anti-vocabulary in the docstring. twin sorry untouched
import EuclidsPath.Engine.Step00PhaseCoverKernel -- 🟢 PHASE-COVER KERNEL (THE P-SCALING WALL BROKEN): certificate-free verified B&B checker (fuel-structural dfsB, exact residual capacity prune, base-2^16 residue-count capQ); two-layer soundness (pure list combinatorics Uncoverable/prune_sound/branch_complete/dfsB_sound + arithmetic bridge dvd_wing/cleanGapBound_of_unsatCover — UNSAT ⟹ CleanGapBound directly for all k, NO periodicity needed); KERNEL TRUE GAPS cleanGapBound_A at A ∈ {17,19,23,29,31,37} (G = 18,25,34,43,58,88) + EXACTNESS PAIRS gapExact_A (G(A) kernel-pinned: CleanGapBound A G ∧ ¬CleanGapBound A (G−1)) + wall instances twinJacobsthal_at_23/29/31/37 + Sharp instances at all six (25 = ⌊361/14⌋ TIGHT at 19); cost ~ B&B tree (464..30079 nodes), not period (P_37 ≈ 1.29e12 — six orders beyond any period certificate); ladder 17 min / 14.4 GB, chunk fallback per gates. twin sorry untouched
import EuclidsPath.Engine.Step00TwinFractalArithmetic -- 🟢 FRACTAL ARITHMETIC (§8.1, green): ForbiddenPhase/SurvivesUpTo/ActiveWindow/ShortSurvivor + shortSurvivor_implies_twin — a center surviving every prime clock 5≤p≤z in the active window 6m+1≤z² is a twin (reuses safeHole_implies_twin; 2,3 never divide 6m±1). This is the real sieve content; it does NOT assert a short survivor EXISTS (that = the twin lower bound / parity barrier)
import EuclidsPath.Engine.Step00SignParityNode -- 🟢 THE ONE BARRIER, NAMED: TwinSurvivorNode (for every horizon an active window has a survivor) — the sign/parity node (four-corner R_fc<1 ↔ Liouville λ; the N=2^k data to k=27 shows B₅=N₀₀−N₃₃>0 with margin 1−R_fc→0⁺). twinSurvivorNode_iff_unboundedTwinCenters proves it EXACTLY = the twin conjecture (BOTH directions green: forward shortSurvivor_implies_twin, backward shortSurvivor_of_twinAbove). All honest routes (relative-index, four-corner, fractal) converge to this one node; it is NOT a shortcut. Not proved here; twin sorry untouched
import EuclidsPath.Engine.Step00FlatCycleEngine -- 🟢 SIGN→ENGINE, engine side machine-explicit: FlatCostFreeCycle (a closed walk that strictly pays rank every step yet returns) = the perpetual engine as a paying loop; no_flatCostFreeCycle proves it impossible (ℕ budget telescoping, axiom-light). Convention: FLAT/zero = collapse = engine; NEGATIVE curvature = live = avoids it. EngineForcing M0 := NoSafeHoleAbove M0 → FlatCostFreeCycle is the engine-explicit form of OrnamentEngineBridge (the wall); twin_of_engineForcing / twinLowersInfinite_of_engineForcingFamily reach twins CONDITIONAL on the forcing family (parity barrier). twin sorry untouched
import EuclidsPath.Engine.Step00SignInvariance -- 🟢 SIGN INVARIANCE ("the start sets the sign, the engine forbids the flip"): to change sign the curvature must cross 0, but 0 = flat = engine (no_flatCostFreeCycle) → never_zero for free; discrete_ivt_neg (base<0 + moves up ≤1/step + never 0 ⟹ stays <0) keeps the sign negative on the whole tail ⟹ twins. twinLowersInfinite_of_signInvarianceCertificate conditional on SignInvarianceCertificate whose base/step_up/neg_forces_twin fields carry the remaining parity barrier (the zero-crossing is handled free by the engine). twin sorry untouched
import EuclidsPath.Engine.Step00SignInvarianceBase -- 🟢 START SIGN CLOSED + barrier collapses to ONE field: with the twin indicator (sign k=-1 if a twin exists above 5+k else 0), twinIndicator_base is green (m=7 → 41,43), and being bounded in {-1,0} makes step_up AND neg_forces_twin green too. So the whole sign-invariance route rests on the single field zero_forces_engine (no twin above → FlatCostFreeCycle = the engine-forcing wall = EngineForcing/TwinSurvivorNode). The start sign is NOT where the difficulty lives. twin sorry untouched
import EuclidsPath.Engine.Step00GapBracket4143 -- 🟢 KERNEL GAP BRACKETS AT 41/43 (the SAT sides): allStruck_41/allStruck_43 — axiom-FREE Bool folds certifying every offset of the 90/102-windows at the solver witnesses r41 = 50077677123072, r43 = 233885349904190 is struck by a clock ≤ A → not_cleanGapBound_41_90 / not_cleanGapBound_43_102 (G(41) > 90, G(43) > 102 kernel-certified). Harness-exact values G(41) = 91, G(43) = 103 (phase_cover_run1.log:105–166, all asserts green); the measured node-count law 464→…→473293→4889831 (A = 17..43) says the EXACTNESS ladder ends here — 43-exactness is ×202 beyond the proven kernel budget (pre-registered deferral, disclosed); at 43 the kernel carries the lower bracket only. twin sorry untouched
import EuclidsPath.Engine.Step00Sharp41 -- 🟢 SHARP WALL AT 41 KERNEL-CERTIFIED (the seventh Sharp scale): cleanGapBound_41_120 by the phase-cover UNSAT ladder at g = 120 = ⌊41²/14⌋ — 385 t5×t7×t11 phase cells (hot cells split by t13) generated from the measured exact tree decomposition (tools/sharp41_cells.json, per-cell sums == undecomposed run EXACTLY), 25 resumable part files, sequential kernel build green end-to-end (tools/sharp41_build.log) → twinJacobsthalSharp_at_41 : CleanGapBound 41 (41²/14) + twinJacobsthal_at_41 free by mono. Sharp SURVIVES its first falsification shot: G(41) = 91 ≤ 120 (ratio 0.758), G(43) = 103 ≤ 132 (0.780), no drift toward violation; kernel bracket G(41) ∈ (90, 120]. Measured law: UNSAT trees SHRINK above G (473293@91 → 34749@120, ×13.6) — the capacity prune bites harder as demand grows. twin sorry untouched
import EuclidsPath.Engine.Step00TwinChainCensus -- 🟢 TWIN-CHAIN COMPLETENESS CENSUS (the repo's FIRST two-directional kernel certificate): twinList_complete_10000 — for EVERY m ∈ [1, 10⁴]: TwinCenterZ m ⟺ m ∈ twinList10k (810 entries; every twin found AND everything found is a twin); the completeness direction rests on the kill/witness divisor side (a nontrivial divisor kills a wing — no primality-test completeness needed, clocks need only 2 ≤ q). Fused checker twinSegB (sorted two-list merge, fuel recursion) + twinSegB_spec (both directions, ONE induction); ten chunk gates by decide +kernel — ALL AXIOM-FREE, 6.9 s total, ~350× under gates. Corollaries kernel-pinned: maxGap = 83 with the ATTAINED theorem (TwinCenterZ 4070 ∧ TwinCenterZ 4153 ∧ nothing between — both directions of the census at work) + twin_in_every_83_window on [0, 10⁴]. Data generator re-verifies every literal by independent trial division + exact checker simulation + corrupted-data negative controls. Anti-vocabulary: finite-range ONLY — no infinitude, no density, the census is a certificate, not evidence. twin sorry untouched
import EuclidsPath.Engine.Step00WingJacobiCollapse -- 🟢 THE QUADRATIC IMAGINARY LAYER OVER THE TWIN PANEL COLLAPSES (certified kill, ledger-grade): the fixed offset 2 destroys the multiplicative independence the Friedlander–Iwaniec mechanism needs — wing_cross_jacobi_right/left (J(6m−1|6m+1) = χ₈'(6m+1), J(6m+1|6m−1) = χ₈(6m−1): cross-symbols collapse to mod-8 phases), wing_reciprocity_trivial (the reciprocity sign is +1 ALWAYS — consecutive integers), center_wing_collapse_odd + left sibling (J(m|wing) = χ₄(m) for odd m: no Legendre oscillation survives anywhere in the panel), spin_fixed_label_periodic_left/right (fixed-label edge spins are level-p² functions — the certified boundary: only GROWING labels can carry content, handled by the peel-bilinear layer). DISCLOSURE: Gauss-sum 'energy' |τ|² = q is a unitary basis change — energy-partition observables are exact linear images of the additive layer. A machine-checked refutation of a proposed lever is a ledger law. twin sorry untouched
import EuclidsPath.Engine.Step00TransferCostume -- 🟢 TRANSFER-OPERATOR COSTUMES NAMED + KAC'S RETURN IDENTITY (the genuinely new exact asset): cleanGapBound_iff_killed_nilpotent — the killed shift K = D(1−s)·R is NILPOTENT with index exactly the gap: CleanGapBound A g ⟺ K^g = 0 (costume #5 of the single wall; there is NO eigenvalue-1 vector, no Perron picture — the perpetual engine of the abelian shift dynamics is nilpotency-broken at exactly the gap), kernel instances at 5/7/11/13 + global forms via clean_add_mul_period; killed_energy_forward/backward (KKᵀ = D in partial-map form: all singular values 0/1 — window-truncated spectra of the abelian layer are TRIVIAL, the spectral-statistics proposal for this object is dead); kac_master + kac_return_identity (GENERAL, green): Σ_{r ∈ cleanResidues} τ(r) = P — first-return times tile the period EXACTLY; mean_gap_exact + max_return_le_of_cleanGapBound: the mean return time is the Hardy–Littlewood local product ∏ q/(q−2) while the wall bounds the max — the gap law is a max-vs-mean statement, exactly framed. twin sorry untouched
import EuclidsPath.Engine.Step00PeelBilinear -- 🟢 PEEL BILINEAR FRAME (the priority imaginary candidate — transitions between primes as edges): peel_parity_transport + peel_sign_flip (EXACT conservation law: every peel transition carries parity charge −1 — the engine-oscillation sign flip in graph form, Ω(wing) = Ω(cofactor) + 1); window_edge_count (non-twin centers ↔ canonical out-edges, DISCLOSED restatement grounding the harness normalization); struck_wing_minFac_le + allStruck_17_r502 + defect_edges_small_labels_17 + defect_window_nonTwin_17 (kernel: defect windows are EXACTLY the windows whose out-edge label spectrum is confined to [5,A] on one wing each — the bilinear face of the defect); peel_target_window/' (exact ℕ-division renormalization intervals: a defect window fans into ≤ π(A) scaled windows W/p — the cascade frame). The B&B tree is a proof-search shadow, not an arithmetic object (disclosed; no Lean statements for tree profiles). Numeric campaign verdict (tools/peel_bilinear_run1.log): edge spins / teleported fresh parity / twin-hit rate ALL PARITY-BLIND-UNDER-ESCALATION-v2 — the blindness theorem EXTENDS to observables supported outside the window. twin sorry untouched
import EuclidsPath.Engine.Step00StrikeFourier -- 🟢 STRIKE FOURIER (THE IMAGINARY-DIVISOR LAYER + THE PERPETUAL-ENGINE PAIR): the imaginary divisor made honest — the prime clock q acting through all q-th roots of unity (AddChar/stdAddChar; the orthogonal transition = character orthogonality sum_mulShift). Rational coherence layer (no ℂ): cleanCount/mainTerm/windowFluct with defect_forces_coherence (a defect window forces the engines into an EXACT coherent conspiracy |Fluct| = mainTerm — the user's 'without a twin the defect spawns a perpetual engine'), oscillation_kills_defect + oscillation_plants_twin (ANY decay below full coherence in ANY window under the ceiling plants a twin), perpetualEngine_iff_wall (DISCLOSURE: the FOURTH costume of the single wall — combinatorial, phase-cover-UNSAT, Jacobsthal-window, now coherence) + ten engineDecay instances rereading the kernel certificates as perpetual-engine impossibility proofs at A = 5..37, escalation to TwinLowers.Infinite from cofinal decay. Fourier body in FULL generality: strike_fourier (E1), exact window-count identity in product form (E2a) and mode-tuple form (E2b via Finset.prod_sum), anchors c(0) = prod(1-2/q), S_W(0) = g, and windowFluct_eq_minorSum — the fluctuation IS the sum of nontrivial engine modes, exactly. NO asymptotics anywhere: the minor-arc BOUND is the parity wall in Fourier clothes (the linear character layer is what Selberg's foil is built from — the identity is green, the bound is the wall). twin sorry untouched
import EuclidsPath.Engine.Step00TwinNavigator -- 🟢 TWIN NAVIGATOR + PATH/DEFECT DICHOTOMY: windowVerdict (green, unconditional — below the ceiling 6(k+g)+1 < A² every window carries a certified twin OR a certified CoverDefect; the path exists or the defect exists, EXACTLY); verdict_exclusive above the scale + the boundary-overlap witness (7,0,1); ten no_long_defect instances (5..37) rereading the kernel gap certificates as defect-length bounds; coverDefect_iff_phaseCover (defect = phase-cover pattern) + kernel demos coverDefect_demo_17/37 from the allStruck SAT data; drainage_downhill (DISCLOSURE: the next twin is unreachable through the peel drainage BY CONSTRUCTION — graph search closes, the scan line is the honest navigator); three-layer navigator nextTwin (fuel scan; cleanListB_complete is the new completeness converse; partial correctness + twin upgrade + conditional totality from CleanGapBound); twinSeq enumerator via Nat.find (computable, StrictMono, twinSeq_surj: enumerates ALL twin centers in order, least-witness — conditional only on the named wall); navigator_iff_unbounded_twins (DISCLOSURE: navigator totality = twin infinitude — packaging, not progress; fuel stays the wall). twin sorry untouched
import EuclidsPath.Engine.Step00SideCollision -- 🟢 THE WING-SIDE COLLISION LAW KERNEL-CERTIFIED (the promised L62 escalation): full-period pgf censuses by decide — twin-class side-hit histogram over period 35 = [8,6,1] = coeffs of (2+x)(4+x), over period 5005 = [640,624,196,24,1] = coeffs of (2+x)(4+x)(8+x)(10+x) (total 1485 = 3·5·9·11 — the L34 kernel constant, machine-welded), clean-class = [1485,1092,274,28,1] = coeffs of (3+x)(5+x)(9+x)(11+x) (total 2880); ALL census gates AXIOM-FREE; spec bridge to Finset.card over Icc + the Clean-13 weld (twinClassB = the house cleanListB literally, rfl); the Fraction corollary machine-tied to the gates: mean side-hit excess twin-class minus clean-class = EXACTLY 67/495 = Σ_{q ∈ {5,7,11,13}}(1/(q−2) − 1/(q−1)) — the exact form of the measured L62 law (+0.1535 at 1e7; the B=13 truncation ≈ 0.13535, tail ≈ 0.0185 disclosed). DISCLOSURES: conditioning is CLASS-exact (mod-q admissibility), not actual primality; a collision/competition law in the L34/L35 family — it does NOT feed the wall (no window-ensemble structure). twin sorry untouched
import EuclidsPath.Engine.Step00ImaginaryCircle -- 🟢 THE IMAGINARY CIRCLE (crown of the reconnaissance pass — the structure UNDER the prime): every odd prime carries TWO circles — the real rotation group F_ℓˣ (order ℓ−1) and the IMAGINARY circle, the norm-one torus of the quadratic extension (order ℓ+1; Quad = mathlib's QuadraticAlgebra over ZMod, decide-friendly — GaloisField deliberately avoided); circle_card: #{qnorm = 1} = ℓ+1 kernel-instanced at 5/11. **circle_lucas — Morrison's p+1 converse, a theorem ABSENT from mathlib, fully proved** (minFac reduction, the same orderOf engine as lucas_primality, Euler split; any coprime d is honest). THE CROWN: twin_iff_double_rotation — TwinCenterZ m ⟺ the center c = 6m is realized as a FULL ROTATION TWICE: really (order c in F_{6m+1}ˣ — mathlib Lucas both ways) and imaginarily (order c on the circle over 6m−1 — our circle_lucas both ways). The dyadic cascade (the user's half→quarter→…): v2_min_split — of the two circles of ANY odd n the entire 2-Sylow depth always lives on exactly ONE (side ⟺ n mod 4); twin_wings_mod4 + twin_exactly_one_sqrt_neg_one (in every twin pair exactly one wing has rational √−1 — the forced orthogonal split of imaginarity; TRIVIALITY-labeled frame arithmetic); twin_dyadic_depth_tombstone (naive equal-depth reading REFUTED at m=1: v₂(4) ≠ v₂(8)); circle_involution (the cascade bottoms at ±1 — the Wilson half-turn, welds to Step00WilsonTurn); dft_fourth_pow (𝓕⁴ = N²·id — the quarter-turn of the additive stack, prime-blind naming plaque). DISCLOSURES: pointwise certificate shape — certificate search = factoring c; nothing feeds the wall; circle orders are frame arithmetic; no canonical pairing between the wings' circles; Gauss sign absent from the pin (skipped). twin sorry untouched
import EuclidsPath.Engine.Step00CircleVolume -- 🟢 CIRCLE VOLUME (the user's key made theorems — THE VOLUME DEFICIT FORCES THE ROTATION SHORT): quadCRT (a composite modulus FRAGMENTS the circle: Quad d (mn) ≅ product — the imaginary circle of a composite is a product of local circles); circle_card_split (residue case: the circle degenerates to the unit hyperbola, volume ℓ−1) + circle_card_semiprime (THE VOLUME FORMULA: volume = ∏(ℓ − χ_d(ℓ)) in 4-case form); circle_full_rotation_iff_prime (THE HEADLINE: a full rotation of order n+1 exists on the imaginary circle ⟺ n is prime — forward = reverse-Lucas existence, backward = our circle_lucas); THE VOLUME-DEFICIT THEOREM: circle_order_bound_semiprime (every norm-one point's order divides the lcm of the LOCAL volumes) + circle_cyclic_budget_semiprime (both local volumes even ⟹ 2·lcm ≤ (p+1)(q+1)) + volume_deficit_identity (2(pq+1) = (p+1)(q+1) + (p−1)(q−1) exactly) ⟹ circle_no_full_rotation_semiprime: NO norm-one point of order n+1 exists on a semiprime modulus — the cyclic budget falls short by ≥ (p−1)(q−1)/2; twin_shared_parents (TRIVIALITY-labeled frame: the right wing's real parents = the left wing's imaginary parents = {2,3} ∪ primes(m)). Kernel demos: circleCountN 35 2 = 36 with max rotation 6 (deficit 30, exponent fold AXIOM-FREE over all 1225 pairs); circleCountN 127 3 = 128 — the Mersenne pure-dyadic volume. DISCLOSURES: volume = finite-torus cardinality; the iff is a pointwise certificate (witness search = factoring n+1); semiprime scope (squarefree stretch skipped, prime powers scoped out); nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.Step00CircleTrace -- 🟢 CIRCLE TRACE (the trigonometric face + the Lucas–Lehmer subsumption): circle_trace_dickson — cos(kθ) = T_k(cos θ) in Dickson form (x^k + star x^k = dickson-eval at the trace; NO division by 2; valid for ALL n, ALL d — prime-blind frame arithmetic, labeled); circle_trace_doubling — the LL step s ↦ s² − 2 IS the circle's angle-doubling (the user's half→quarter→… tower read from the top: quarter-turn with trace 0 at step p−2, half-turn −1 at p−1, full turn at p); xEquiv (mathlib's LucasLehmer.X q IS Quad 3 q, componentwise rfl); ll_test_iff_half_turn (+ the honest modulus-gap completion: the half-turn forces order 2^p, cardinality forces mersenne prime); **ll_sufficiency_via_circle — the STRICT SUBSUMPTION plaque: the Lucas–Lehmer test derived through OUR circle_lucas only (never invoking mathlib's lucas_lehmer_sufficiency) — LL is circle_lucas at the pure-dyadic volume M+1 = 2^p, where the entire imaginary circle IS the dyadic cascade**. Kernel demo cascade_walk_127 ([propext] only): the full cascade walk on M7 = 127 — sModNat 127 5 = 0, trace-zero quarter-turn at 2^5, half-turn −1 at 2^6, full turn at 2^7 — and prime_127_via_cascade: (127).Prime END-TO-END through circle_lucas, consuming no primality anywhere (unit −2 by the explicit inverse 4·32 ≡ 1). DISCLOSURES: mathlib owns BOTH LL directions and the quarter-turn trace lemma (ω_pow_trace) — this module adds the circle READING, not novelty; nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.Step00CircleEnergy -- 🟢 CIRCLE ENERGY + THE KLOOSTERMAN BRIDGE (the exact-cancellation layer): circle_re_pair_count (#{re-collision pairs on the circle} = 2ℓ exactly, χ-fiber route); THE ENERGY IDENTITY — Σ_{u≠0} ‖circleSum u‖² = ℓ² − 2ℓ − 1 EXACTLY (the Weil bound's AVERAGE case as a finite identity: the mean square of the circle's trigonometric sums is ~ℓ — no cohomology, built from AddChar.sum_mulShift alone); THE BRIDGE circleSum u = −kloos(u/2, u/2) (the def kloos is NEW to the pin, grep-verified) — the circle's trigonometry IS Kloosterman territory, machine-welded via the fiber identity N_conic(a) + N_hyperbola(a) = 2 for every a; the FAMILY second moment kloos_family_M2 = ℓ² − ℓ − 1 (conjugation-correct form kloos(a,1)·kloos(−a,−1)) + the BONUS twisted law kloos_family_twisted = −(ℓ+1) (caught by the stage-D probe) + kloos_scale (the M4 module's input, exact shape). Kernel: pairCountN 5/11 = 10/22 ([propext] only). DISCLOSURES: pointwise Weil 2√ℓ NOT claimed (absent from the pin); these are COMPLETE sums over full residue classes — the wall lives on INCOMPLETE window sums, and the complete/incomplete boundary is the wall's SIXTH costume (the stage-D probe measured NO stable re-expression of defect-window pair-mode mass in complete kloos data — circle_sum_run1.log). twin sorry untouched
import EuclidsPath.Engine.Step00LargeSieve -- 🟢 THE ADDITIVE LARGE SIEVE (new to mathlib — zero hits in the pin; THE FIRST ESTIMATE-TYPE THEOREM ON THE INCOMPLETE SIDE): large_sieve_v0 — Σ over ALL level-1 mode frequencies of ‖expSum a K (j/q)‖² ≤ (K + maxRowSum A)·Σ‖a‖², with the EXPLICIT exact constant maxRowSum A = Σ_{q′} A·q′·harmonic(⌊Aq′/2⌋) (ℚ-exact; ≈ 2204.21 at A = 13, kernel-anchored) — one inequality binding ALL clocks at once on incomplete window sums (the user's 'relation strict, exact, at scale' in inequality form); the duality-free Schur chain with the exact 1/(2δ) kernel atom (Jordan) and the row sums organized by the Farey class bijection (no sorting); mode_mass_large_sieve — the program corollary on modeWave objects (trivial-bound comparison disclosed). THE SATURATION ANATOMY AS THEOREMS: level_wall witnesses (level-2 spacing 1/1001 < 1/169 at scale 13 — the same method costs ≫ K exactly one level below the measured conspiracy) + universality_blindness (the inequality holds verbatim under Selberg-foil weights — Type-I machinery is DEFINITIONALLY parity-blind, now a theorem-form instance); clock_parseval (the exact per-clock identity whose naive clock-summation loses what the sieve recovers). v1 Gallagher (K + O(A²)) deferred with TODO disclosure. Measured (dispersion_run1.log s4): the bound holds everywhere with saturation 0.003–0.016 (harmonic constant dominates — disclosed); level-2 cost 86× (A=13) → 3272× (A=43) the ceiling. twin sorry untouched
import EuclidsPath.Engine.Step00WindowDispersion -- 🟢 WINDOW DISPERSION (the headline of the dispersion pass): pairCorr_eq_prod — THE EXACT CRT PAIR-CORRELATION LAW: #{doubly-clean pairs at distance d per period} = ∏(q − cq(d)) with the exact trichotomy cq ∈ {2,3,4} (the overlap classes are 3d ≡ ±1 mod q — this CLOSES ledger goal L9: the pair generalization of admissible_card); window_variance_exact (the exact second moment over the period — pure double counting, kernel-pinned at 13: VarSum = 593854/91); **defect_windows_rare — THE FIRST QUANTITATIVE LAW ABOUT THE WALL'S OWN OBJECTS: defect windows (perpetual engines) are RARE per period — (defectCount)·mainTerm² ≤ the exact closed-form variance, unconditional, every quantity a closed rational** (Markov against defect_forces_coherence); **cleanGapBound_5_via_dispersion : CleanGapBound 5 2 — the ONLY scale where the averaging route genuinely crosses the wall** (Markov gap 5/9 < 1; the measured table shows B(G) ≥ 1 for every A ≥ 7, blindness ~2·10⁷ by A = 29 — dispersion_run1.log); engine energies (total_engine_energy = M₁ − M₁², the exact Bernoulli-variance anchor of the Wall_L analysis); dispersion_identity (the K-shift Fluct²-sum reorganized EXACTLY into mode cross-terms with geometric kernels); THE DEATH CERTIFICATE of naive shift-dispersion: near_diagonal_no_decay (Re D_K ≥ K/2 within 1/(6K) of the diagonal) + the CRT frequency bijection + freq_density_near_diagonal (≥ P/(3K) − 1 no-decay partners per row; at scale 13: ≥ 99) — the ceiling segment is exponentially shorter than the period and the near-diagonal carries no decay. DISCLOSURES: the dispersion-blindness law is a law about a ROUTE (L53 genus), not a costume; Markov is the AVERAGE form — pointwise remains the wall; no Kloosterman arises from clean-indicator dispersion (linearity blocks it). twin sorry untouched
import EuclidsPath.Engine.Step00FareyModeGeometry -- 🟢 FAREY MODE GEOMETRY (the user's 'infinitely subdividing a sphere with a relation strict, exact, at scale' — honest home): the program's level-1 mode frequencies j/q are Farey fractions over the clocks, and the strict exact relation at EVERY scale is unimodularity/Bezout (= Ford-circle tangency in classical dress) + CRT injectivity. level1_spacing_wraparound (true distance-to-nearest-integer form: every translate ≥ 1/qq′ — the δ-input for the large sieve); farey_adjacent_exists_unique (EXACTLY ONE unimodular pair per ordered clock pair per sign); **crossterm_class_bijection (BijOn, the load-bearing lemma): every admissible Farey class m = jq′−j′q occurs EXACTLY ONCE per clock pair — replaces sorting arguments in the large-sieve route and organizes the dispersion cross-terms into m-classes** (admissibleClasses_ncard = (q−1)(q′−1)). THE APOLLONIAN KILL as machine lemmas: mediant_denom_even + mediant_denom_not_clock — the mediant/Stern–Brocot subdivision does NOT act on the clock set (mediant denominators are even, never clocks): the Descartes/Apollonian angle is dead by theorem, not by taste. Kernel demos at (5,7): the unimodular pair (3,4) with uniqueness over all 24 pairs, 24 distinct classes mod 35 — decide, unimodular_5_7 axiom-FREE. DISCLOSURES: Ford/sphere language is translation — the exact content is Bezout/CRT; frame infrastructure, no wall contact claimed. twin sorry untouched
import EuclidsPath.Engine.Step00KloostermanMoment -- 🟢 THE KLOOSTERMAN FOURTH MOMENT — THE PROGRAM'S FIRST MACHINE-CHECKED NONTRIVIAL POINTWISE CANCELLATION BOUND: kloos_family_M4 — Σ_{c≠0} kloos(1,c)⁴ = 2ℓ³ − 3ℓ² − 3ℓ − 1 EXACTLY (the family fourth moment; assembly: double orthogonality over both frequencies → ℓ²·N4, boundary peel (ℓ−1)⁴ + 2(ℓ−1), kloos_scale multiplicity, cancel ℓ−1), with kloosN4_card = 3(ℓ−1)(ℓ−2) counted by the Vieta completeness spine (equal keys with s ≠ 0 force multiset equality of root pairs; s = 0 forces the antipodal family — a CLEANER route than the numeric blueprint's χ-key table, disclosed; max case nesting 2) → **kloos_norm_le : ‖kloos a b‖⁴ ≤ 2ℓ³ for ab ≠ 0 — Kloosterman's 1926 elementary ℓ^{3/4} bound against the trivial ℓ, fully machine-checked** → circleSum_norm_le via the bridge: the imaginary circle's trigonometric sums obey the same cancellation. Kernel demos n4CountN 5/7 = 36/90 ([propext], Fermat-inverse folds). DISCLOSURES: COMPLETE sums over full residue classes ONLY — the wall lives on incomplete window sums (the sixth costume, complete/incomplete boundary); pointwise Weil 2√ℓ NOT claimed (Deligne territory, absent from the pin); nothing here feeds the wall — this is the first proven-cancellation ASSET of the program, not a wall route. twin sorry untouched
import EuclidsPath.Engine.Step00WilsonTurn -- 🟢 WILSON TURN (the number BEFORE the prime): the factorial signature of the ENTIRE past of n takes exactly two values — wilson_dichotomy: ((n−1)! mod n) = −1 ∧ prime XOR = 0 ∧ composite (n > 4) — the composite→prime transition is the forced jump 0 → −1, annihilator → HALF-turn (the first factorial infrastructure in the repo; composite_dvd_pred_factorial is the side absent from mathlib). CLEMENT'S CRITERION (1949, absent from mathlib, now green): clement_iff — the pair (n, n+2) is twin ⟺ ONE congruence n(n+2) ∣ 4((n−1)!+1)+n; center form clement_center: TwinCenterZ m ⟺ (36m²−1) ∣ 4((6m−2)!+1)+(6m−1) — the modulus is c²−1, the wing product. Kernel artifact of a NEW type: clemFoldB (pure Nat mulmod fold, no primality test anywhere in the checker) with AXIOM-FREE kernel certificates twin_center_17 (pair 101/103) and the stretch twin_center_9945 (pair 59669/59671, ~6·10⁴ mulmods, 2 s) + negative controls. DISCLOSURES: Clement is pointwise Wilson×2 — a costume with named value; it can NEVER feed the wall (nothing summable over windows; (n−1)! is the compressed sieve of the whole past); −1 is the half-turn, NOT the quarter-turn (the quarter-turn lives in Step00ImaginaryCircle). twin sorry untouched

-- GEOMETRIC TWIN-PRIME PROGRAM (rigorous [A] geometry from geometric_twin_prime_program_full.md + the §79–83 conditional skeleton). Parallel analytic formulation of the same descent; genuinely-new connected/analytic identities the repo lacked, plus one forward micro-advance and the honest route classification. All green/axiom-clean; twin sorry and step00FirstCause untouched.
import EuclidsPath.Engine.GeometricSegreDefect -- 🟢 SEGRE CONNECTED DEFECT (the strict projective upgrade of FourCorner): segre_decomposition (A·P = LR + Δ(1−x)(1−y)), cov_eq_delta_div_Asq (Cov = Δ/A², Δ = TQ−UV), exterior_algebra_identity (2(AS−M₋M₊) = ΣΣ w w (x−x)(y−y) — the connected defect is an exact connected energy; pure-left/pure-right deformations tangent to the Segre quadric), crossRatio/hilbertDist and hilbertDist_eq_zero_iff_segre (projective separation from rank one vanishes exactly on AS = M₋M₊). DISCLOSURE: the four-corner gap in energy form, same single wall; nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.GeometricPrimeProjector -- 🟢 PRIME PROJECTOR + SOFT PROJECTOR: sum_signed_pow_card (Σ_{T⊆P}(−t)^|T| = (1−t)^r — the squarefree Möbius generating identity Σμ(d)t^{ω(d)}=(1−t)^r), prime_projector_derivative (−Σμ(d)ω(d) = 1_{r=1} — the linear order selects primes), softProjector_bounds (1_ℙ(n) ≤ t^{Ω(n)−1} ≤ 1_ℙ(n)+t, the engine of the reduction skeleton), gammaOmega_eq (per-prime pair Euler factor v/u² = 1−(1−t)²/(p−1+t)²). DISCLOSURE: exact identities of the divisor hypercube; nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.GeometricFactorBox -- 🟢 FACTOR-BOX GEOMETRY: factorBox_volume (∏1/qᵢ = 1/n), factorBoxPoly_semiprime (Φ_{pq} = 1+(p+q)u+pq·u²), factorBox_disc_semiprime ((p+q)²−4pq = (p−q)²) + factorBox_balanced_iff (vanishes iff p=q, the multiple-root locus), vandermonde_sq (the discriminant shape ∏(qⱼ−qᵢ)² is the Vandermonde square), anisotropy2_eq_cosh (I₂ = cosh(½log(q/p))) + anisotropy2_ge_one. DISCLOSURE: classifies the SHAPE of composite defects (near-balanced semiprimes at the isoperimetric diagonal); nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.GeometricTwinShellFourier -- 🟢 TWIN-SHELL / SHAPE-LINE / LOG-DIVISOR FOURIER: twin_defect_law (E_j = g_j+1−N_j), gap_from_volume (g_j = (c_j c_{j+1}/6)ΔV_j), shape_line (u+v = η_c := ½log((c+1)/(c−1)) — all divisor reps of one center on one line) + hodge_reflection (centered Hodge complement is x↦−x), logDivisor_fourier (the centered log-divisor Fourier sum = ∏cos(ξ log p/2), the exact Fourier image of the factorization hypercube). DISCLOSURE: §58 telescoping equivalence is tautological packaging (not reproduced); the 2/p spectral law is the repo's existing ±6⁻¹ classes; nothing feeds the wall. twin sorry untouched
import EuclidsPath.Engine.GeometricDiagonalCovariance -- 🟢 DIAGONAL COVARIANCE MADE EXACT & UNCONDITIONAL (§44 [B]→[A], the one forward micro-advance): Dwing_mul_zero (for p≥5 the wings never coincide, D⁻·D⁺=0 — p∤2), diag_cov_eq_neg_prod (the diagonal covariance is EXACTLY −E[D⁻]E[D⁺], sign-definite, no positive part), diag_cov_period_exact (period-exact = −1/p², NO error term — the honest replacement for §44's conditional one-prime density input), inv_sq_le_telescope (1/p² ≤ 1/(p−1)−1/p, the diagonal telescopes/summable). DISCLOSURE: a real sharpening of the TARGET, isolating the off-diagonal barrier — NOT a move of the wall (the diagonal was never the wall). twin sorry untouched
import EuclidsPath.Engine.GeometricReduction -- 🟢 THE §79–83 REDUCTION AS A GREEN CONDITIONAL: twinCenter_of_window (positive prime marginals + a connected-defect bound ≤ c²/4 on the soft projector, small fixed t ⟹ a twin center in the window — the machine form of §29/§83 via the soft-projector sandwich), twinCenters_cofinal_of_reduction (inputs beyond every horizon ⟹ ∀ N ∃ m>N, IsTwinCenter m). ONE-DIRECTIONAL sufficient condition, NOT an iff with the conjecture; every o(1) is an EXPLICIT finite hypothesis; uses neither step00FirstCause nor twin_prime_conjecture. twin sorry untouched
import EuclidsPath.Engine.GeometricRouteClassification -- 🟢 WHERE THE ROUTES MEET (honest, NOT "one wall, not three"): twins_of_geometricMixingTarget (🟢 conditional — the geometric mixing target ⟹ infinitely many twin centers; hypothesis strictly stronger than twins), mixing_forces_fourCorner (🟢 conditional, FORWARD ONLY — a strictly negative connected defect forces N₃₃<N₀₀, reusing N33_lt_N00_of_four_corner: the geometric barrier lands on RealFourCorner's exact named open input). DISCLOSURE: offDiagDecay (§45/§81) stays 🔴 OPEN, NOT stated as an iff with SerialTwinBoundary; cofinal-information reading = ParityBarrier.sound_cert_requires_cofinal_information; nothing moves the wall. twin sorry untouched

-- TYPE-II PARITY-WALL MAP (parity_wall_twin_primes_research_dossier → Lean): the honest map that always shows WHERE to hit. The dossier's deep Type-II program (Liouville → clocks → FI/KBSZ/Ford–Maynard → Y_p=G_p+Z_p → S_{2k} Pythagoras 1/p→1/p^{2k−1} → CRE/PFCLS) localizes the parity wall to ONE explicit target. Green exact identities (annihilation/gain/reduction), green no-go markers (where the wall is / closed routes), and the single 🔴 CRE target with a green-conditional reduction to twins. All axiom-clean; twin sorry and step00FirstCause untouched.
import EuclidsPath.Engine.GeometricTypeIIProjection -- 🟢 EXACT LOCAL TYPE-I PROJECTION Y_p=G_p+Z_p (§35/§113): Drow (row wing-indicator 1_{m·n+2≡0}), Drow_mul_zero (distinct rows ⟹ product 0), two_row_cov = −1/p² (§113.1, exact two-row diagonal covariance), two_row_cov_survival = −1/(p−1)² (§113.2) + two_row_summability (Σ1/(p−1)²<∞), Zrow_mean_zero (the pure nonprincipal residual has zero full mean — exact annihilation on full CRT, criterion A). Generalizes Geometric.DiagCov to two arbitrary rows. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIICycle -- 🟢 EXACT CYCLE/COLLISION/CRT-ROOT LAWS (§27/§75/§87): det_bridge (m₂·k₁h₁−m₁·k₂h₂=2(m₂−m₁) — long variable cancels, criterion C), kbsz_shell (r·v−s·u=2(r−s), fixed determinant shell), same_prime_collision (a common prime forces p|A(s−r)ℓ — same-clock kill §28), crt_root_sq (C≡1(r),C≡−1(s) ⟹ C²=1 in ZMod(rs), §87), crt_root_phase (Σ+ΔC ≡ 2m₂(q₁), 2m₁(q₂)). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIQuartic -- 🟢 S_{2k} SUPPORT PYTHAGORAS + SUMMABILITY GAIN 1/p→1/p³ (§38–42): zEnergy_S4 ((p−2)/(p−1)^4 ≤ 1/(p−1)^3 — the exact 1/p→1/p³ transition, criterion B) + zEnergy_cube_summable (telescopes, Σ<∞), disjoint_support_orthogonal (distinct supports don't interact), pythagoras_of_orthogonal (orthogonal family ⟹ Σ(Σf_i)²=ΣΣf_i² — the support Pythagoras, §41). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIINoGo -- 🟢 THE NO-GO MAP (where the wall is / anti-renaming gate): mixed_mode_parity_scale (1/(2p) ≤ zEnergy 1 p — the S_2 residual energy is parity-DIVERGENT Σ1/p=∞, the wall's exact local form §72.1), zEnergy_is_gain/typeII_genuine_progress (the S_4 energy IS a summability gain, criterion B machine-checked). Documented CLOSED routes (§51/§99/§104): Möbius/Chowla trap, DHR collapse, scalar-BV non-tensorization, false √(m₁m₂) wall. GenuineProgress = the anti-renaming gate (A annihilation ∨ B summability gain ∨ C dimension reduction, §110). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIMap -- 🟢 THE SINGLE OPEN TARGET + REDUCTION (§97/§101/Appendix E): CRE/PFCLS (🔴 named open Prop — the cross-modulus CRT-root energy 𝔈≪_B x²(log x)^{−B}, the ONE unproven analytic input, where to hit), twins_of_typeII (🟢 conditional — the bundled Type-II program CRE + named Ford–Maynard chain ⟹ ∀N ∃m>N IsTwinCenter m; one-directional, strictly stronger than twins). STATUS registry in the module docstring. Ford–Maynard step = named hypothesis requiring external audit (§105). Uses neither step00FirstCause nor twin_prime_conjecture. twin sorry untouched

-- TYPE-II PRIME-CHAOS SESSION DOSSIER (Parity_Wall_Prime_Chaos_Session_Dossier.md → Lean): the full honest map of the prime-chaos / parity-wall session (67 §). Green exact identities + no-go theorems + named open residuals + a SECOND green-conditional twin route. Localizes the wall to two explicit objects (degree-two c=pq core + low-frequency CRT-root remainder). NOT a proof of twins — the dossier proves it isn't. All axiom-clean; twin sorry and step00FirstCause untouched.
import EuclidsPath.Engine.GeometricTypeIICRT -- 🟢 FULL-CRT MARGIN + MÖBIUS–CRT RESIDUAL (§2–4): two_wing_ratio_factor (1−2/p=(1−1/p)²(1−1/(p−1)²)), telescope_prod_Icc + crt_two_wing_margin (∏(1−1/(p−1)²)>3/4 by subset domination — two local wing prohibitions are compatible with a fixed positive margin), crt_margin_gap (A²−B<¼A²), moeb_pair_coprime, MoebiusCRTResidual (the literal parity wall as a named structural object). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIGram -- 🟢 EXACT GRAM MATRIX + ZERO-SUM ISOMETRY (§5–7): gram_diag=p(p−2)/(p−1)³, gram_off=−p/(p−1)³ (the exact Gram entries), zero_sum_isometry (on Σc=0, ‖Σc_aZ_a‖²=(p/(p−1)²)Σc_a² — Z is the local parity component, NOT small), mean_mode_energy=1/(p−1)³ + mean_parity_orthogonal (the summable p^{−3} mean mode ⟂ the divergent p^{−1} parity mode). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIISpectrum -- 🟢 J−I GRAPH + SURVIVOR OPERATOR SPECTRUM (§14–17): survivorOp_const=(q−1)/q, survivorOp_meanzero=−1/q (the exact spectrum of T_p=(J−I)/q), centeredSurvivorOp_meanzero=−1/(q−1) (the fixed-point parity eigenvalue −1/(p−2)), zEnergy_eq_schatten (zEnergy k p = the Schatten 2k-energy Tr|Z|^{2k}). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIChaos -- 🟢 PRIME-CHAOS DECOMPOSITION + CRITICAL S₂/S₄ THRESHOLD (§18–21): schatten_powerset (∏(1+a_p)=Σ_S∏_{p∈S}a_p), schatten_S2_lower (1+Σ1/(p−2)≤∏(1+1/(p−2)) — S₂ dominates the divergent partial sum, UNBOUNDED), schatten_S4_bounded (∏(1+1/(p−2)³)≤exp(1/2) — S₄ UNIFORMLY BOUNDED). The quadratic level is on the parity boundary; the quartic has a summable budget. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIFixedPoint -- 🟢 ONE-DEFECT FIXED POINT NO-GO (§26–28): one_defect_fixed_point (Σ_χ λ(χ)^{2k−1}λ(χθ)=(−1/m)Σ_χ|λ(χ)|^{2k} — the parity eigenvalue −1/(p−2) is reproduced at EVERY even moment). Higher moments S₄,S₆,S₈,… alone cannot break the wall (§59). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIForest -- 🟢 FOREST RECURSION + EXACT NONLOCAL CLOSURE (§30–36): forest_telescope (F_J−1=Σ_j c_j F_{j−1}, leaf-stripping), soft_euler_factor (1+θc_p=a_p(1−g_p·1_{p|L})), soft_euler_telescope (a_p(1−g_p/(p−1))=1 — the main-term collapse to X_γ), local_rigidity (the unique normalized hard annihilator is (p−1)/(p−2)), soft_factor_bound (forbidden soft factor in [0,1−θ]). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIConductor -- 🟢 DEGREE-TWO CONNECTED CORE = WALL CENTER (§41–50): degree_two_S2_mass=(Σ1/(p−2))² (diverges — the wall), degree_two_S4_mass=(Σ1/(p−2)³)² (converges — summable budget), pythagorean_two (2 log p log q=(log p+log q)²−S₂), signed_partition_sign (μ(u)μ(v)=μ(uv)), degreeTwoCore (the minimal connected core R^{[2]}, current 🔴 wall center §66). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIRoots -- 🟢 CRT-ROOT FOURIER MOMENTS + ROOT S₄ THRESHOLD (§52–55): root_additive_energy_local (local additive energy of {±1} is 1²+2²+1²=6), root_S4_euler (Σ_{q sqfree}6^ω/q³=∏(1+6/p³)), root_S4_bounded (∏(1+6/p³)≤exp 6 — root S₄-budget BOUNDED), PairRoughRemainder (named structural §52), LowFreqRootCoherence (🔴 named open Prop, residual E §55). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIILiouville -- 🟢+🔴 ONE-WING TWIN CRITERION = 2nd 🔴 TARGET (§10–13): prime_liouville_form (1_ℙ(n)=(1−(−1)^Ω)/2 for Ω∈{1,2}), one_wing_lower (T≥−½(L₋+L₊)), OneWingTarget (🔴 named open Prop — pair-rough positivity + negative parity bias, cofinally), twins_of_oneWing (🟢 conditional — the SECOND explicit twin route parallel to CRE). Uses neither step00FirstCause nor twin_prime_conjecture. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIKloosterman -- 🟢 KLOOSTERMAN / FOUR-CORNER GEOMETRY (§22–25): graph_eq_kloosterman (graph=Kloosterman bridge, reuse circleSum_eq_neg_kloos), three_component_count (|V_p|=3(p−1)(p−2), reuse kloosN4_card), kloosterman_weil_bound (‖Kl‖⁴≤2p³, Weil strength, reuse kloos_norm_le), four_corner_det (det=2A(r₁−r₂)(ℓ₁−ℓ₂), new) + four_corner_rank_one (rank-one degeneracy). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIWallMap -- 🟢+🔴 CAPSTONE HONEST MAP (§56–68): divisor_switch_returns_survivor (§60 — the high-divisor Möbius tail returns the survivor, switching alone closes nothing), SemiprimeShortRestriction/HigherConductorDispersion (🔴 CRE-shape residuals C/D §65), twins_of_final_package (closing OneWingTarget ⟹ twins — the final logical route without hidden Bridge §68). STATUS registry §63/§64/§65/§66 in the docstring. NOT a proof of twins: the wall is localized to two explicit objects, not defeated. twin sorry untouched

-- WALL-CENTER ASSAULT (dossier §67, the machine-checkable part of "the next mathematical strike"): the S₄-opening program at the degree-two wall center, executed in its formalizable part. Adversarially verified constants (exact rational + exhaustive brute force). Every result AMBIENT; §57 (ambient S₄ does not transfer to the short rectangle) repeated in every disclosure; NO new open Props — the wall stays exactly at SemiprimeShortRestriction/HigherConductorDispersion/LowFreqRootCoherence/CRE/OneWingTarget.
import EuclidsPath.Engine.GeometricTypeIIBilinear -- 🟢 THE TWO-VARIABLE TYPE-II GAIN AT FULL PERIOD (§9): bilinear_block_bound (|B_p| ≤ 3RL/(p−1) for 7≤p, p≤R, p≤L — via class energies ≤2R²/p + Cauchy–Schwarz against gram_quadratic_form), bilinear_gain_summable ((B/RL)²≤9/(p−1)² telescopes — criterion B at the BILINEAR level), Zrow_forbidden_eq (wall visibility: the residual at the forbidden cell is order ONE — the gain is full-period averaging, never pointwise smallness), degenerate_pair_energy (§25 counting input). Hypotheses p≤R, p≤L are LOAD-BEARING (R=L=1 gives |B|=1): the sub-period regime IS the wall, unchanged. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIDegreeTwo -- 🟢 DEGREE-TWO ONE-DEFECT CONTRACTION + CONNECTED BUDGET (§51/§67 steps 3–4): degree_two_one_defect_contraction (the [3+1]-defect sector of the (p,q) layer = (−1/m)·(even moment at p)·(pure degree-one sum in q) — chaos degree 2→1 EXACTLY, criterion C; with the §26 profile table s4_profile_22/211/1111 + bounds in GeometricTypeIIFixedPoint: among off-diagonal profiles ONLY [3+1] is non-summable), degree_two_connected_budget (≤36/(m³n³), criterion B). AMBIENT ONLY (§57): does NOT close the degree-two layer — the ambient-to-short transfer IS the wall (SemiprimeShortRestriction); landing degree-one layer closed only under standard large-sieve inputs. Local root moments rootSum_M2/M4 (=2p/6p, §53 ω=1) in GeometricTypeIIRoots. twin sorry untouched

-- WALL-CENTER STRIKE II (the wall made unhideable + the exact short-restriction apparatus): the literal §3 wall formula as a machine THEOREM about real twin counts, the §55 completion toolkit on residual E's own object, and the §10 rough-set grounding of the one-wing route. All exact identities / visibility lemmas; ZERO new open Props; per-q absolute bounds are the known dead end (signed coherence = residual E, transfer = residual C — both unchanged).
import EuclidsPath.Engine.GeometricTypeIICompletion -- 🟢 EXACT COMPLETION APPARATUS OF THE SHORT RESTRICTION (§55): interval_completion (short sum = (1/q)Σ_h W_M(h)F(h) EXACTLY, all q, ALL M), interval_weight_geom/_bound (W_M(h)(ψ(−h)−1)=ψ(−hM)−1, ‖W‖·‖ψ(−h)−1‖≤2 — algebraic min(M,q/‖h‖), no sine estimates), interval_parseval (Σ_h‖W_M(h)‖²=qM EXACTLY for M≤q — necessary), rootFourier_parseval (Σ_h‖S_q(h)‖²=q·|{C²=1}|, general q), root_remainder_fourier (q·(short root count)=M·|R|+Σ_{h≠0}W·S EXACTLY — residual E's literal per-q object), root_remainder_prime_bound (≤√(2M) at prime p, M≤p — WORSE than trivial 2 for M>2, recorded for structure, NOT criterion B). Signed low-frequency coherence (LowFreqRootCoherence) untouched. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIWindow -- 🟢 THE LITERAL PARITY-WALL FORMULA AS A MACHINE THEOREM (§2–3): twin_sieve_window_identity (actual twin-center count of a critical-range window = the exact alternating Möbius–CRT double sum Σ_{S,T}(−1)^{|S|+|T|}N_{S,T} with REAL strike counts), prod_one_sub_expand (overlapping pairs vanish = EXACTLY no_large_shared_divisor), survivor_iff_twin (the Legendre criterion as a full iff, reuse Residuals.sink_is_twin), prod_dvd_iff ((S,T) = coprime Möbius pairs (∏S,∏T)). VISIBILITY step (§110: not A, not B, not C): the wall's own formula grounded as real arithmetic — bounds NOTHING, residuals C/D/E and CRE unchanged; the wall can no longer hide behind the free-standing def MoebiusCRTResidual. twin sorry untouched

-- BOUNDARY-MOVE PROGRAM (strike 1 of the "Сдвиг границы" plan): the completion×Weil engine + two §110 criterion-C events on REGISTERED targets (B: OneWingTarget shrinks to ONE sign condition, in GeometricTypeIILiouville; the CRT junction root_card_crt in GeometricTypeIIRoots). Conditional surface shrinks on the flank; the signed Möbius core is untouched.
import EuclidsPath.Engine.GeometricTypeIIIncomplete -- 🟢 THE COMPLETION×WEIL ENGINE — FIRST UNCONDITIONAL SUB-PERIOD CANCELLATION: intervalWeight_eq_expKernel (bridge to the existing Dirichlet-kernel machinery — no Jordan re-derivation), intervalWeight_L1 (Σ_{h≠0}‖W_M(h)‖ ≤ p(1+log p), M-uniform), kloos_norm_le_uniform (‖Kl(c,a)‖ ≤ 2^{1/4}p^{3/4} for ALL c), incomplete_kloos_bound (‖Σ_{m<M}ψ(a/m+bm)‖ ≤ 2^{1/4}p^{3/4}(M/p+1+log p) UNCONDITIONALLY), incomplete_inv_sum_bound (sharp b=0 form), circle_short_window_count (short-window equidistribution of the circle/inversion geometry ≤ 2^{1/4}ℓ^{3/4}(1+log ℓ)). DISCLOSURE: nontrivial only for M ≫ p^{3/4}log p; fourth-moment Weil strength (3/4, NOT full Weil 1/2 — gap stated); single prime modulus; a §64 named input discharged = conditional-surface reduction, NOT a §110 event; residuals C/D/E and CRE untouched. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIISpecial -- 🟢 SPECIAL COEFFICIENTS ARE EASY (the warning that guards the wall): bilinear_v_one_elementary (|B_p| ≤ (3/2)R + (⌊R/p⌋+1)L/(p−1), ALL R,L — NO period hypotheses; contrast bilinear_block_bound's load-bearing p≤R, p≤L), box_count_identity (the u=v≡1 block IS the hyperbola box count — bilinear frame = circle/hyperbola geometry, exactly), hyperbola_box_count (|N_box − RL/p| ≤ (3/2)R + (⌊R/p⌋+1)L/p unconditional), class_count_dev (|L − pN| ≤ p). CLOSED ROUTES recorded: one-short-variable Cauchy–Schwarz backfires (variance loses below the period — the wall showing itself); general-coefficient completion worse than elementary; p^{3/4} double-completion window deferred with thresholds. The sieve needs general μ-SIGNED coefficients — for those nothing below full period is claimed: that IS the wall. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 1a (idea-generation session: the geometry axis). The three-key twin variety — the kloosN4 blueprint with BOTH twin poles x and x+2 in one complete-sum object. Green-front growth of the complete-sum core: exact identities + an averaged Markov count; NOT a §110 event — no registered target touched, the wall stands at SemiprimeShortRestriction/HigherConductorDispersion/LowFreqRootCoherence/CRE/OneWingTarget exactly as before.
import EuclidsPath.Engine.GeometricTypeIITwinVariety -- 🟢 THE THREE-KEY TWIN VARIETY + EXACT FAMILY FOURTH MOMENT: twin_fiber (THE UNIFORM FIBER LAW — the third key (x+2)⁻¹+(z+2)⁻¹ cuts kloosN4's antipodal fattening from fiber ℓ−1 to fiber 2: Vieta spine for x+z≠0, corner spine 4/(4−x²)=4/(4−q₁²) for x+z=0), twinN4_card (N4=(ℓ−2)(2ℓ−5) — cleaner than Kloosterman's 3(ℓ−1)(ℓ−2), no stratum trisection), twinV_family_fourth_full (triple orthogonality Σ_{a,b₁,b₂}V²V(−)²=ℓ³·N4), twinV_family_M4_norm (Σ‖V‖⁴=ℓ³(ℓ−2)(2ℓ−5) EXACT ≤ 2ℓ⁵), twinV_markov (≤2ℓ²/K bad wing-frequency pairs — the cash value: joint two-wing fourth-moment smallness for almost all (b₁,b₂)), kernel demos twinN4CountN 5/7 = 15/45 + two-key control 53≠45 at ℓ=7 (key-3 necessity witnessed; ℓ=5 coincidence 15=15 disclosed). DISCLOSURES: complete sums only, one prime modulus; NO pointwise extraction (no scaling law — the moment's pointwise consequence 2^{1/4}ℓ^{5/4} is WORSE than trivial ℓ−2, deliberately not stated; the honest analogue of "Layer 4 does not port"); μ-signed aggregation untouched. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIITwinWindow -- 🟢 THE SHORT-WINDOW CONSEQUENCE — JOINT TWO-WING CANCELLATION AT THE ℓ^{2/3} THRESHOLD (almost all wing pairs): twin_short_completion (S(b₁,b₂)=(1/ℓ)Σ_h W_M(h)·V(h,b₁,b₂) EXACT all M — the completion frequency lands on the family's linear slot), weighted_cs_fourth ((ΣwU)⁴≤(Σw)³(ΣwU⁴) — double Cauchy–Schwarz in fourth-power form, NO roots; the pre-pass CORRECTION: naive L¹×L∞ gives only ℓ^{3/4}, the double CS is load-bearing for 2/3), twin_short_fourth_bound (good pairs: ‖S‖⁴ ≤ (M/ℓ+1+log ℓ)³·K·M·ℓ²), twin_short_good_pairs (HEADLINE: explicit bad set ≤2ℓ²/K, outside it the window bound holds — o(M) exactly when M ≫ K^{1/3}ℓ^{2/3}(2+log ℓ), prose reading). DISCLOSURES: crossing module (leaves the pure chain via Incomplete); ALMOST ALL pairs, never all (all-pairs at 2/3 via this route would be FALSE to state; Salié/Weil = possible future upgrade); one prime modulus, unsigned phases — μ-signed aggregation and all registered targets untouched, NOT a §110 event. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 1b (idea-generation session: the algebra axis). The dilation ladder — the entry identity of the UNIQUE sign-odd exact operation lambda(pn) = −lambda(n) (exchange is sign-even: neg_one_pow_cardFactors_exchange — exchange-invariant constructions cannot see parity). Identities + wing asymmetry, NOT estimates; the ceiling of known math on this mechanism is log-averaged two-point Chowla; face B's cofinal sign untouched.
import EuclidsPath.Engine.GeometricTypeIIDilation -- 🟢 THE DILATION LADDER + μ=λ ON THE ROUGH MINUS WING: strikePairs (the {(p,n): pn=N} Finset, exactly ω(N) members — N≠0 LOAD-BEARING, at N=0 the pair set is infinite), strikePairs_lambda_sum (Σ_{strikes}λ(n) = −ω(N)λ(N) — one rung down, forced sign flip), wing_dilation_identity (THE LADDER over any window, with the EXPLICIT (Ω−ω)λ correction — the correction-free Ω-version is FALSE, fails on non-squarefree wings), wing_dilation_identity_of_squarefree (correction vanishes), strike_congruence (mod-6 rigidity: strikes pair classes (1,5)/(5,1)), minus_wing_no_square (x² ≡ 5 mod 6 IMPOSSIBLE — the wing asymmetry), rough_minus_squarefree + rough_minus_moebius_eq_liouville (μ = λ FOR FREE on the rough minus wing — face B's Liouville data = the μ-frame of C/E there; roughness LOAD-BEARING: moebius_ne_liouville_125, 125 = 5³), rough_minus_semiprime_factor (Ω=2 wing element = p·q, p≠q, BOTH rough), rough_window_omega_lambda_sum (2ΣΩλ = |I| + 3L, integer-safe) + rough_window_ladder_cross_check (= 3·#composite − |I|, agrees with rough_window_liouville_sum exactly), rough_plus_square_thin + plus_wing_square_exists (plus wing DOES carry prime squares, confined to y<p, p²<y³). DISCLOSURES: identities not estimates; visibility only, NOT a §110 event — no registered target moved. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 2 (idea-generation session: no-go hardening). Three machine no-gos on the sign side — all born as adversarial REFUTATIONS of attack seeds; zero movement by definition (§110), map-hardening only.
import EuclidsPath.Engine.GeometricTypeIISignBlind -- 🟢 THE SIGN-BLINDNESS NO-GO TRIPLE: cross_conductor_sign_eating (μ(q)·B_q(j) = A_q(j)/(q−2) > 0 at EVERY prime conductor and EVERY moment index — the Möbius sign is EXACTLY eaten by the parity eigenvalue −1/(q−2); μ-telescoping across the one-defect tower is structurally impossible; concrete Fin(m+1) model instantiating one_defect_fixed_point), one_defect_tower_ge_S2 (Σ 1/(p−2) ≤ Σ μ(p)B_p(0) — the signed tower DOMINATES the divergent S₂ partial sum: the "Euler-derivative" is S₂ renamed), signed_conjugation_trace_blind (Tr((D_ε A D_ε)^k) = Tr(A^k) for any ±1 diagonal — ALL spectral moments are sign-blind; the μ-signs enter the Gram frame only as such a conjugation), exchange_invariant_sign_eq_one_or_liouville (COMPLETENESS: a completely multiplicative sign function constant on primes is 1 or (−1)^Ω — the exchange filter admits EXACTLY two sign characters; size weights and dilation are the only sign-visible mechanisms, now certified not just observed). DISCLOSURES: no-gos move nothing — no registered target touched; three prose-level closed-route annotations are now machine theorems. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 3 STAGE 1 (idea-generation session: the formalization-first track). The Bonferroni bridge into mathlib's DORMANT Selberg-sieve frame — sieve infrastructure; Brun's method is an upper-bound method, structurally blind to the parity wall (that is WHY it can be completed unconditionally); nothing here bounds a twin quantity yet.
import EuclidsPath.Engine.GeometricTypeIIBrun -- 🟢 THE BONFERRONI BRIDGE: alternating_partial_binomial (Σ_{j≤t}(−1)^jC(k,j) = (−1)^tC(k−1,t) — each truncation closes in ONE Pascal term), squarefree_prod_of_primes, moebius_truncated_divisor_sum (CLOSED FORM: Σ_{d∣n, ω(d)≤t} μ(d) = (−1)^tC(ω(n)−1,t) exactly — non-squarefree die, squarefree divisors ↔ prime subsets, powerset sliced by card), bonferroni_isUpperMoebius (EVEN t ⟹ mathlib's BoundingSieve.IsUpperMoebius holds for μ(d)·[ω(d)≤t] — siftedSum_le_mainSum_errSum_of_upperMoebius is now APPLICABLE with finite inclusion–exclusion order; first usable key to the dormant frame). STAGE 2 NAMED, NOT CLAIMED: twin BoundingSieve instance (ν=2^ω/d via root_card_crt), e_j ≤ (Σx)^j/j! tail tool, qualitative density conclusion (pin has not_summable_one_div_on_primes but NO quantitative Mertens — ceiling disclosed). NOT a §110 event; no registered target touched. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIICocycle -- 🟢 THE CRT COCYCLE OF THE STANDARD CHARACTER (face-E infrastructure): crt_cocycle_dvd (q₁q₂ ∣ u₁q₂ + u₂q₁ − 1 with u₁ = q₂⁻¹ mod q₁, u₂ = q₁⁻¹ mod q₂ — the partial-fraction cocycle 1/(q₁q₂) = u₁/q₁ + u₂/q₂ − e closes with an integer), stdAddChar_crt_factor (ψ_{q₁q₂}(x) = ψ_{q₁}(u₁x)·ψ_{q₂}(u₂x) EXACT for all x, coprimality the only hypothesis — the pin has NO such factorization; verified numerically incl. the non-prime pair (4,9)). DISCLOSURES: infrastructure only — a per-q refactorization of the frequency language; μ-signs across moduli untouched by construction (an exact rewrite cannot create cancellation); intended downstream (NOT claimed): the two-frequency form of the semiprime remainder for face E. NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwin -- 🟢 BRUN STAGE 2 — THE TWIN SIEVE INSTANTIATED: twinSieve (mathlib's dormant BoundingSieve filled: support = values (6m−1)(6m+1) with injective value map, ν(d) = 2^ω(d)/d multiplicative via prodPrimeFactors pdiv id, sifting primes EXACTLY [5,z] — p=2,3 never divide 36m²−1 AND the frame forbids them), root_res_card (2^ω ROOT COUNT: #{r mod d : d ∣ 36r²−1} = 2^ω(d) — rootFourier_card_prime × root_card_crt × the unit-6 bridge), twinSieve_rem_bound (|rem d| ≤ 2^ω(d), one unit per root class via the pure-ℕ residue_class_bracket), brun_twin_upper (THE UNCONDITIONAL BOUND: #twins(N) ≤ N·mainSum(μ⁺_t) + errSum(μ⁺_t) + z/6 + 1 for ALL N,z, even t — pre-pass verified in exact rationals at 7 points; CORRECTED boundary split at 6m−1 > z, the 6m+1 split is FALSE at z=29,m=5), twin_errSum_le (errSum ≤ Σ 2^ω symbolic), esymmOn_le_pow_div_factorial (TAIL TOOL e_j ≤ (Σx)^j/j! via (j+1)e_{j+1} ≤ (Σx)e_j double counting — the stage-3 instrument, proved now). DISCLOSURES: still NOT a density theorem — mainSum/errSum symbolic, stage 3 must pick z,t and evaluate (pin ceiling: qualitative, no quantitative Mertens); Brun structurally blind to the parity wall; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIISalie -- 🟢 THE CLASSICAL SALIÉ EVALUATION (all-frequency, one pole): salie_eval (THE IDENTITY T(a,b) = χ(a)·g(χ)·Σ_{v²=ab}ψ(2v) — pre-pass CORRECTED: the χ(a) twist is LOAD-BEARING, without it the identity is false at ℓ=7,a=3,b=6; verified 8e−15 at ℓ=3..23; route = completing-the-square + fiberwise regroup, the delta-trick is CIRCULAR and unused; fiber_count #{w:(w+v)²=tw} = χ(t(t−4v))+1), salie_eq_zero_of_nonsquare (nonsquare ab kills T), gauss_norm (‖g(χ)‖=√ℓ — derived, NOT in the pin), salie_norm_le (‖T(a,b)‖ ≤ 2√ℓ for ALL a,b≠0 — pointwise, no averaging, no Weil input), salie_sq_norm_le (‖T‖² ≤ 4ℓ, numerically sharp 3.93ℓ at ℓ=23). DISCLOSURES: ONE POLE ONLY — the twin two-pole family V(a,b₁,b₂) does NOT reduce to Salié, its all-frequency problem at exponent 1/2 remains open; the twin variety keeps its almost-all Markov statement; unsigned phases, one prime modulus, μ-signed aggregation untouched; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwinDensity -- 🟢 BRUN STAGE 3 — **TWIN PRIMES HAVE DENSITY ZERO, MACHINE-CHECKED**: twin_mainSum_eq (EVENIZATION: t = 2k makes the Bonferroni truncation VACUOUS — mainSum = ∏_{5≤p≤z}(1−2/p) EXACTLY via the pin's prodPrimeFactors_one_sub_of_squarefree, no tail estimate needed), twin_errSum_le_const (errSum ≤ 3^k, N-FREE — the whole point), exists_sievePrimes_sum_ge (Σ1/p unbounded — pin divergence bridged through the indicator, 5/6 bookkeeping for dropped p=2,3), exists_sievePrimes_prod_lt (the Legendre product dies: 1−x ≤ e^{−x} under the divergent sum), twin_count_le_eps (WORKHORSE: ∀ε ∃N₀ ∀N≥N₀ twins(N) ≤ εN), **twin_density_zero** (HEADLINE: twins(N)/N → 0, Tendsto/atTop/nhds 0 — the first standalone classical theorem of the formalization-first track; squeeze route REFUTED by the pre-pass, metric characterization used). DISCLOSURES: qualitative only (pin has no Mertens — no rate; true rate C·N/log²N named, not claimed; esymmOn tail tool stays dormant = stage-4 instrument); an upper bound says NOTHING about infinitude — Brun structurally blind to the parity wall; NOT a §110 event. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 4 (session continuation: the in-repo composition + the redesigned Segre-Farey brick). Visibility passes: exact identities on the wall's faces, no target moved.
import EuclidsPath.Engine.GeometricTypeIILadderWindow -- 🟢 THE RUNG LADDER — face B in Buchstab form: minus_wing_rung_criterion (POINTWISE IFF: Ω(N)=2 ∧ minFac N = p ⟺ p ∣ N ∧ (N/p).Prime ∧ p < N/p on N ≡ 5 mod 6 — p.Prime load-bearing in ⟸ (N=725,p=25), mod 6 in ⟹ (N=25,p=5); strictness = the wing's no-square asymmetry; reuses rough_minus_semiprime_factor at y:=0 where roughness degenerates), minus_window_semiprime_rung_decomposition (THE FIBERING, roughness-FREE: semiprime count of ANY minus-wing window = Σ_p rung counts by smallest prime), rough_window_prime_semiprime_split (P+S=|I| — no repo lemma existed), rough_minus_window_rung_ladder(+_grounded) (THE HEADLINE: Σλ(6m−1) = Σ_p R_p − P — the Liouville bias IS the excess of cofactor-prime counts at scales X/p over the prime count at scale X; grounded from 6m−1 < y³ + roughness), rung_count_eq_cofactor_count + mem_rung_cofactor_image (q-form: each rung literally counts primes q > p with pq on the wing; m = (pq+1)/6 recovers). Pre-pass: real window |I|=103, P=74, S=29, Σλ=−45=29−74; fibering 29=9+8+4+4+3+1 over {11,13,17,19,23,29}; on a RAW window the fibering holds but the headline FAILS (−24 ≠ −12) — the dichotomy is load-bearing exactly where disclosed. DISCLOSURES: identity not estimate — no R_p or P is bounded, the SIGN of face B's premise is exactly as open as before; NOT a §110 event, visibility only. twin sorry untouched
import EuclidsPath.Engine.GeometricSegreFarey -- 🟢 THE SEGRE×CRT FLAT-MODE ANNIHILATION (the wave-3 Segre–Farey brick, REDESIGNED by the pre-pass — the literal panel statement is termwise-trivial and kept only as the flagged display lemma segre_flat_tangent): crt_sum_factor (CRT FUBINI: Σ_{x mod q₁q₂} f₁(x₁)f₂(x₂) = (Σf₁)(Σf₂) — the pin has no such lemma), crt_sum_marginal_left/right, exterior_energy_kills_flat_modes (THE ANNIHILATION: the exterior/Segre energy of any purely-left × purely-right field pair is EXACTLY 0 over the complete semiprime period — COLLECTIVE cancellation, 840/1225 terms individually nonzero at (5,7); the complete CRT frame sits ON the Segre quadric; via Segre.exterior_algebra_identity), two_freq_orthogonal (Fourier slice: only the doubly-flat mode (0,0) survives at full period — the dictionary partner of stdAddChar_crt_factor). DISCLOSURES: COMPLETE PERIOD ONLY — the short-window version is FALSE (measured defect −2,−6,−12 at M=12,16,23 for (5,7)): that nonzero short-window defect IS face E — this module CONFINES the open content to the incommensurable short window, it does not reduce it (root_remainder_full_period disclosure pattern); the row-frame two-row energy deliberately NOT re-recorded (invertible rescaling of two_row_cov — double-counting); NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwinRate -- 🟢 BRUN STAGE 4a — THE QUANTITATIVE RATE, ONE NAMED INPUT AWAY: twin_count_of_prod_le (THE PARAMETRIC RATE, unconditional: any product bound B at level z gives twins(N) ≤ B·N + 3^z + z/6 + 1 — stages 1–3 glued into one consumable inequality), twin_count_log_rate (THE CONDITIONAL RATE: under the NAMED Mertens-type hypothesis ∏_{5≤p≤z}(1−2/p) ≤ C/log²z — classical, NOT in the pin, entering explicitly — twins(N) ≤ C·N/log²z + 3^z + z/6 + 1 for ALL N, z ≥ 2; prose reading: z ≈ ½log N gives the crude Brun rate O(N/(log log N)²)). The house conditional-theorem pattern (as twins_of_typeII/CRE): stage 4 reduced to ONE classical named input; discharging it (or importing a future mathlib Mertens) is the named remaining step. DISCLOSURES: the Mertens hypothesis is NOT proved here; stage 4b (true Brun rate via truncation + the dormant esymm tail) NOT claimed; upper bounds only — Brun structurally blind to the parity wall; NOT a §110 event; the Mertens input is a theorem HYPOTHESIS, not a named open Prop. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwinMertens -- 🟢 THE MERTENS INPUT DISCHARGED — **THE BRUN RATE IS UNCONDITIONAL**: sum_Icc_inv_le_prod_primesBelow (the FINITE Euler expansion dominates the harmonic sum — the pin's summable_and_hasSum_smoothNumbers_prod_primesBelow_geometric at the hand-built invCastHom, no s>1 truncation issue), prod_primesBelow_le_inv_log (∏_{p≤z}(1−1/p) ≤ 1/log z via log_add_one_le_harmonic), sievePrimes_prod_inv_eq (the EXACT p=2,3 peel: factor 3), sievePrimes_prod_le_nine_div_log_sq (THE DISCHARGE: ∏(1−2/p) ≤ 9/log²z for all z≥2 — the SQUARING TRICK 1−2/p ≤ (1−1/p)², difference exactly 1/p², deletes the whole −log(1−x)≤x+x² + Σ1/p² + exp apparatus; pre-pass: sup_z LHS·log²z = 2.497, C=9 with 3.6× margin), **twin_count_log_rate_unconditional** (twins(N) ≤ 9·N/log²z + 3^z + z/6 + 1 for ALL N, z ≥ 2 — stage 4a CLOSED unconditionally; prose: z ≈ ½log N gives the crude Brun O(N/(log log N)²)). DISCLOSURES: constant 9 not optimal (truth 4e^{−2γ} ≈ 1.26) — not claimed, not needed; stage 4b (true rate via truncation + dormant esymm) named, unclaimed; upper bounds only — Brun structurally blind to the parity wall; NOT a §110 event. twin sorry untouched

-- TWO-AXES PROGRAM, WAVE 5 (autonomous continuation under the standing mandate): the composite-modulus complete-sum core + the quantitative Brun arc.
import EuclidsPath.Engine.GeometricTypeIITwinVarietyCRT -- 🟢 THE TWIN VARIETY ON SQUAREFREE MODULI: twinDomU (the UNIT twin domain {x : IsUnit x ∧ IsUnit(x+2)} — the naive x≠0 domain is REFUTED at q=35: 33 ≠ 15 at zero frequency, all 42875 triples violate; x≠0 is NOT CRT-componentwise, units ARE), twinDomU_eq_twinDom + twinVU_eq_twinV (PRIME BRIDGE: nothing changes at primes, total reuse), castHom_inv_of_isUnit (unit inverses map componentwise — 10 nonunit counterexamples mod 35 make the hypothesis load-bearing), **twinVU_crt_factor** (THE TWISTED FACTORIZATION: twinVU_{q₁q₂}(a,b₁,b₂) = twinVU_{q₁}(u₁a₁,u₁b₁₁,u₁b₂₁)·twinVU_{q₂}(u₂a₂,u₂b₁₂,u₂b₂₂), cocycle units on all three frequency slots, domain untwisted — verified 6.5e−15 at (5,7), 2.0e−14 at (5,11), ALL triples), twinVU_family_fourth_full + M4_norm_general (M4U(q) = q³·N4U(q) at EVERY modulus — ψ primitive at all q), **twinN4U_card_crt** (N4U multiplicative via the quadruple CRT bijection — the prime uniform fiber law does NOT port: composite fibers {1,2,4}), twinVU_family_M4_semiprime (M4U(ℓ₁ℓ₂) = (ℓ₁ℓ₂)³·∏(ℓᵢ−2)(2ℓᵢ−5)). DISCLOSURES: complete sums, unsigned, full boxes — infrastructure for the composite-modulus core; faces C/E (short windows, μ-signed aggregation) untouched; coprimality the only hypothesis of factorization/multiplicativity; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwinMertensUpper -- 🟢 BRUN STAGE 4b-A — MERTENS' SECOND THEOREM, UPPER FORM: sum_primesLE_inv_abel (the EXACT Abel identity Σ_{p≤⌊x⌋}1/p = θ(x)/(x·log x) − ∫₂ˣ f′θ against the pin's dormant sum_mul_eq_sub_integral_mul₁ — first use of the AbelSummation frame in the repo), sum_primesLE_inv_le (**MERTENS UPPER**: Σ_{p≤x}1/p ≤ log4·loglog x + log4·(1/log2 − loglog2) ≈ 1.386·loglog x + 2.509 for ALL x ≥ 2 — Route A: Chebyshev's θ ≤ x·log4 fed straight into Abel, the Legendre/factorial route skipped; the boundary term cancels the antiderivative's −1/log x EXACTLY, no o(1) bookkeeping), sievePrimes_sum_inv_le (over the twin-sieve prime set), sievePrimes_sum_two_div_le (THE 4b INPUT, rounded via log_two d9 bounds: Σ_{sievePrimes z} 2/p ≤ 3·loglog z + 6 for z ≥ 3 — the exact x of the truncation tail Σ_{j>t}x^j/j!). DISCLOSURES: constant log4 from Chebyshev, NOT optimal (Mertens 1874: loglog + 0.2615 — not claimed, not needed); no PNT input anywhere; infrastructure for stage 4b, upper bounds only — Brun structurally blind to the parity wall; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIBrunTwinTruncated -- 🟢 BRUN STAGE 4b — THE TRUNCATED SIEVE (the named remaining step of the Brun track, CLOSED): sum_divisors_sieveProd_eq (the stage-1 divisors↔subsets bijection made reusable for ANY summand — P(z) squarefree, no kill-step), twin_mainSum_sub_prod_le (THE TRUNCATION DEFECT: mainSum(μ⁺_t) ≤ ∏(1−2/p) + Σ_{j∈(t,k]}e_j(2/p) — the defect −Σ_{ω(d)>t}μν bounded by the unsigned esymm tail, |μ|=1 on squarefree divisors), esymm_tail_le (THE EXPONENTIAL TAIL: Σ_{j>t}e_j ≤ e^{3X}/2^{t+1}, X = Σ2/p — e_j ≤ X^j/j! (the stage-2 dormant tool, now CONSUMED), (t+1)!(j−t−1)! ∣ j! splits off the (t+1)-st term, (2X)^{t+1}/(t+1)! sits inside the exponential series), twin_mainSum_truncated_le (mainSum ≤ ∏ + e^{3X}/2^{t+1}), twin_errSum_truncated_le (THE POLYNOMIAL ERROR: errSum(μ⁺_t) ≤ (t+1)·(2z)^t via C(k,j) ≤ k^j/j! — in place of the stage-3 3^{π(z)}), **twin_count_brun_truncated** (THE TRUNCATED BRUN BOUND: twins(N) ≤ N·(9/log²z + e^{9·loglog z+18}/2^{t+1}) + (t+1)(2z)^t + z/6 + 1 for ALL N, z ≥ 3, even t — 4b-A's Mertens upper feeds the tail exponent; prose reading, NOT Lean-stated: 2^t ≈ log^{11}z, z ≈ N^{1/(4t)} give the TRUE Brun shape O(N(loglog N)²/log²N)). DISCLOSURES: constants crude and not optimized (disclosed thresholds astronomically large, as for every honest Brun chain — the value is the parametric family); upper bounds only, Brun structurally blind to the parity wall; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIITwinVarietyCRTMarkov -- 🟢 WAVE 5b — THE EXCEPTIONAL FREQUENCY SET LEAVES THE PRIME LINE: twinVU_moment_crt_factor (**THE MOMENT FACTORIZATION**: Σ_a‖V_{q₁q₂}(a,b₁,b₂)‖⁴ = Σ_{a₁}‖V_{q₁}(a₁,u₁b₁,u₁b₂)‖⁴ · Σ_{a₂}‖V_{q₂}(a₂,u₂b₁,u₂b₂)‖⁴ for coprime moduli and EVERY pair — the a-slot twist absorbed by a unit bijection; a pair is exceptional mod q₁q₂ IFF its cocycle-twisted components are jointly exceptional; verified numerically at (5,7) over ALL 1225 pairs, worst 2.0e−15), twinVU_markov_general (Markov at EVERY modulus: #{(b₁,b₂): Σ_a‖V‖⁴ ≥ Kq³} ≤ N4U(q)/K), twinVU_family_M4_semiprime_le (M4U(ℓ₁ℓ₂) ≤ 4(ℓ₁ℓ₂)⁵, 7.3× slack at (5,7)), twinVU_markov_semiprime (THE SEMIPRIME COUNT: ≤ 4q²/K at q = ℓ₁ℓ₂ — the wave-1 prime-line exceptional-set count now living at a composite modulus). DISCLOSURES: complete sums, full boxes, unsigned; the SHORT-WINDOW transfer at composite modulus NOT ported (completion engine is prime-line; its composite port = named unclaimed work); no pointwise extraction exists for this family — the averaged count is the entire cash value; faces C/E untouched; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIITwinWindowU -- 🟢 WAVE 5c — THE SHORT WINDOW LEAVES THE PRIME LINE: intervalWeight_L1_general (the kernel L¹ bound Σ_{h≠0}‖W_M(h)‖ ≤ q(1+log q) at EVERY modulus — the wave-1 prime hypothesis was INESSENTIAL: hypothesis change, same harmonic-halves proof), twinShortU + twinShortU_eq_twinShort (the unit-guarded two-wing window; at primes IT IS the wave-1 object), twin_short_completion_U (S_U = (1/q)Σ_h W_M(h)V_U(h,b₁,b₂) EXACT for all M at all moduli — unit-domain guard is the correct composite guard, wave-5a lesson; verified numerically at q=35 incl. M>q, worst 1.8e−15), twin_short_fourth_bound_U (moment ≤ Kq³ ⟹ ‖S_U‖⁴ ≤ (M/q+1+log q)³KMq² — the weighted double Cauchy–Schwarz chain, every modulus), twin_short_good_pairs_U (bad set ≤ N4U(q)/K at every modulus), **twin_short_good_pairs_semiprime** (THE HEADLINE: at q=ℓ₁ℓ₂, ≤ 4q²/K bad pairs outside which the q^{2/3}-regime window bound holds — joint two-wing short-window cancellation at a COMPOSITE modulus, almost all pairs). DISCLOSURES: threshold reading prose-only (M ≫ K^{1/3}q^{2/3}(2+log q), constant 2→4); this puts the short-window LANGUAGE at composite moduli — face E's actual content (μ-SIGNED aggregation across INCOMMENSURABLE moduli) untouched: one composite modulus is still ONE modulus; no pointwise extraction (wave-1 disclosure stands); NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIKloostermanCRT -- 🟢 WAVE 5d — KLOOSTERMAN'S 1926 MULTIPLICATIVITY, MACHINE-CHECKED: kloosU + kloosU_eq_kloos (the unit-domain Kloosterman sum; at primes IT IS the repo's kloos), **kloosU_crt_factor** (THE 1926 FACTORIZATION: Kl_{q₁q₂}(a,b) = Kl_{q₁}(u₁a₁,u₁b₁)·Kl_{q₂}(u₂a₂,u₂b₂) with the partial-fraction cocycle units — the one-pole port of the wave-5a factorization, classical Kloosterman 1926/Salié 1932, the pin has NO Kloosterman sums at all; verified numerically at (5,7) over ALL 1225 pairs, worst 1.1e−14), kloosU_norm_semiprime (‖Kl_{ℓ₁ℓ₂}(a,b)‖⁴ ≤ 4(ℓ₁ℓ₂)³ for componentwise nondegenerate (a,b) — the prime-line fourth-moment bound per component, 3.8× slack at (5,7)), kloosU_norm_semiprime_rpow (THE HEADLINE FORM: ‖Kl‖ ≤ √2·q^{3/4} at q = ℓ₁ℓ₂). DISCLOSURES: 3/4 is the FOURTH-MOMENT exponent, not Weil's 1/2 (prime-line disclosure stands — the pin has no Weil bound, none claimed); degenerate strata (frequency vanishing mod a factor) behave differently (Kl(0,0) mod ℓ = ℓ−1) and are NOT bounded here — named, not claimed; unsigned complete sums, one composite modulus — composite-core infrastructure, wall faces UNTOUCHED; NOT a §110 event. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIISalieCRT -- 🟢 WAVE 5e — SALIÉ'S 1932 MULTIPLICATIVITY, MACHINE-CHECKED: salieU + salieU_eq_salie (the JACOBI-twisted sum on the unit domain — Jacobi = the CRT-compatible extension of Legendre, IN THE PIN with mul_right/mod_left'; at odd primes it IS the wave-3 salie, the private quadratic character reached by definitional unfolding), **salieU_crt_factor** (THE 1932 FACTORIZATION: T_{q₁q₂}(a,b) = T_{q₁}(u₁a₁,u₁b₁)·T_{q₂}(u₂a₂,u₂b₂) — the character splits WITHOUT a twist (jacobiSym.mul_right), the frequencies carry the cocycle units; verified at (5,7) over ALL 1225 pairs, worst 7.5e−15), **salieU_norm_semiprime** (THE HEADLINE: ‖T‖ ≤ 4√q at q = ℓ₁ℓ₂ for ALL componentwise nondegenerate pairs — a POINTWISE EXPONENT-1/2 bound at a COMPOSITE modulus, no averaging; 1.37× slack at (5,7) — sharp-ish, unlike the fourth-moment routes). DISCLOSURES: exponent 1/2 inherited from the prime-line classical Salié evaluation (elementary), NOT from Weil — the pin has neither, both live in this repo; ONE POLE with χ-twist — the twin two-pole family does NOT reduce to Salié (wave-3 disclosure stands verbatim), the twin family keeps its almost-all Markov statement; componentwise nondegeneracy on BOTH slots required, degenerate strata named not claimed; unsigned complete sums, one composite modulus — composite-core infrastructure, wall faces UNTOUCHED; NOT a §110 event. twin sorry untouched

-- WALL-ASSAULT CAMPAIGN (this session: 8 pin auditors -> 6 attack panels -> 16 designs -> 3-lens adversarial verification -> synthesis). Verdict recorded honestly: NO wall-break exists in the pool; three registered-quantity events ranked for build (hyperbola reciprocity on face E's dyadic slices; the first Lean large sieve; the face-B Mertens package turning the disclosed S13 heuristic into a theorem). The judge-mandated FIRST step is the gate-protection module below.
import EuclidsPath.Engine.GeometricTypeIIMachineEnergy -- 🟢 THE GATE-PROTECTION STEP — the abstract 𝔈-slots receive a CONCRETE MACHINE REFERENT: machineE (THE CANONICAL ENERGY: squared μ-signed collective window defect of the CRT square roots of unity over squarefree conductors coprime to 6 — PairRoughRemainder at the TRUE root-class defects, window = conductor level = ⌊x⌋) with ω-slices machineE₂ (face C) / machineE₃ (face D); sanity layer: abs_rootClassDefect_le_one (each root-class window defect ≤ 1 pointwise — ALL content is in the signed aggregation), card_natRoots_eq_pow_omega (exactly 2^ω(q) root classes — the pure-ℕ bridge to sq_roots_card_prod), pow_omega_le_self_of_squarefree, machineE_le_envelope (explicit finite envelope (Σ 2^ω)²); canonical instantiations MachineCRE / MachineSemiprimeShortRestriction / MachineHigherConductorDispersion (the SAME registered targets, now machine-adjudicable); **THE FREE-SLOT DISCLOSURE AS FOUR THEOREMS**: CRE/SemiprimeShortRestriction/HigherConductorDispersion/LowFreqRootCoherence all HOLD VACUOUSLY at 𝔈 = 0 (three campaign designs died exactly by self-chosen surrogate energies — the hole is now machine-documented; any §110 claim on a face must name its 𝔈). Numeric grounding: at N=20/50/120 the signed remainder is −1.11/−1.88/−2.29 against envelopes 13/31/93 — the asserted cancellation is numerically visible and NOT proved here. DISCLOSURES: this module proves NO face and closes NO face — it grounds the targets; the vacuity theorems are disclosures, not progress; face E's low-frequency referent lands with the hyperbola module (campaign SPEC 1). twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIHyperbola -- 🟢 SPEC 1 UNSIGNED LAYER — THE SAWTOOTH AND THE HYPERBOLA SWITCH: count_res_one/count_res_neg_one (exact progression counts on [1,M]: class 1 has ⌈M/d⌉ = (M+d−1)/d members, class −1 has (M+1)/d — explicit bijections), trivialRootShift + trivialRootShift_eq_sawtooth (the C=±1 sector of the machine root shift is the EXACT sawtooth ⌈M/d⌉+⌊(M+1)/d⌋−2M/d), trivialRootShift_eq_zero_of_dvd (full periods have ZERO defect — the block object is pure short-window content), natRoots_prime + rootShift_eq_trivial_of_prime (THE PRIME-SLICE PIN: at prime conductors p ≥ 5 the trivial-root sector IS the full machine referent — mixed roots live only at composite conductors, ceded to faces C/D), filter_mul_lt_eq_range + ceil_div_le_of_le, **hyperbola_switch** (THE SWITCH: Σ_{d∈S} w(d)⌈N/d⌉ = Σ_{k<⌈N/D⌉} Σ_{d∈S, kd<N} w(d) EXACT — the Dirichlet hyperbola double count, unbounded variable eliminated). Pre-pass (exact rationals): counts+sawtooth exhaustive d≤40, M≤200 — 0 violations; switch exhaustive N≤300, 7 block sizes, random signed weights — 0 violations. DISCLOSURES: identities not estimates; for D ≤ √M the dual is LONGER (no reduction claimed there); the signed consequences live in the sibling. twin sorry untouched
import EuclidsPath.Engine.GeometricTypeIIHyperbolaSigned -- 🟢 SPEC 1 SIGNED LAYER — **THE SAWTOOTH-TO-MÖBIUS DIMENSION REDUCTION (criterion C on face E's registered carrier)**: blockConductors + blockRemainder (= PairRoughRemainder (trivialRootShift M) at the dyadic block — the definitional carrier tie), moebiusCut (THE CANONICAL 1-DIM OBJECT: Möbius partial sums of the block below dual cutpoints), **signed_block_dual** (THE EXACT DUAL IDENTITY: blockRemainder M D = Σ_{k<K₁} cut(M,k) + Σ_{1≤k<K₂} cut(M+2,k) − 2M·Σ_{block} μ(d)/d — the μ-signed dyadic block of face E's trivial-root sector equals K₁+K₂−1 one-dimensional Möbius partial sums plus one explicit main term; pre-pass: exact-Fraction verification on 6×6 (M,D) grid — 0 violations), abs_blockRemainder_le_of_cut_bound (THE HONEST OPEN CORE ISOLATED: any uniform bound B on the cuts gives |blockRemainder + main| ≤ (K₁+K₂)·B — the named hypothesis IS the canonical open object), dual_dimension_le (K₁+K₂ ≤ 2(M/D)+3) + dual_dimension_lt_interval (for 2(M/D)+3 < D the dual count is STRICTLY below the block interval length — machine profile at M=10⁶: 125892 conductors → 16 dual terms at D=M^0.85). §110 FILING: criterion C on the dyadic-block slice of PairRoughRemainder — the bilinear sawtooth object replaced EXACTLY by the canonical one-dimensional object. DISCLOSURES (campaign-mandated): the unsigned block sum is NOT O(M/D) (smooth bias at D ≫ M^{2/3} — only the exact dual formula is the theorem); D ≤ √M claims nothing; the surviving open core (Möbius partial sums at cutpoints) is EXACTLY as open as face E's dyadic slice — THIS MODULE CLOSES NO FACE; mixed CRT roots ceded by name (ω=2 → SemiprimeShortRestriction, ω≥3 → HigherConductorDispersion); nothing smuggled. twin sorry untouched

-- TAUBERIAN CAMPAIGN (Newman's analytic theorem -> PNT -> PNT mod 6 -> Liouville cancellation -> face-B wing sign). Stage N: the analytic core in Laplace coordinates, built on the pin's Morera/wedge frame (HasPrimitives, lifted from PNT+). HONEST FRAME fixed before construction: OneWingTarget is NOT discharged and NOT shrunk by this campaign (any instantiation forces twin-count > 0 by pigeonhole - it is twin-strength); the landing proves per-wing sign theorems and re-grades the S13 descriptor only.
import EuclidsPath.Tauberian.StadiumPrimitive -- 🟢 STAGE N-A — MORERA ON THE NEWMAN STADIUM (the campaign's #1 risk, built first): stadium R δ = ball 0 R ∩ {Re > −δ} (open, convex via reCLM half-plane preimage), gap-lemma (ball z (gap z) ⊆ stadium replaces all of mathlib's ball arithmetic; gap = min (R−‖z‖) (Re z+δ)), mix_mem_stadium (the one nontrivial corner estimate (‖z‖+ρ)² < R² by nlinarith), horiz/vert_seg_subset_stadium (axis-parallel segments through the pin's Convex.rectangle_subset), **wedgeIntegral_sub_wedgeIntegral_stadium** (THE 8-INTEGRAL LEDGER, ported from the pin's PRIVATE eventually_nhds_wedgeIntegral_sub_wedgeIntegral: wedge(0,w) − wedge(0,z) = wedge(z,w) eventually near every stadium point — splitting I₁=I₃+I₇, I₂=I₈+I₆ plus one Goursat rectangle), IsConservativeOn.hasDerivAt_wedgeIntegral_stadium (the 0-anchored primitive differentiates: the pin's PUBLIC ball theorem at center z + path-independence transport), **DifferentiableOn.isExactOn_stadium** (THE DELIVERABLE: a holomorphic function on the stadium has a primitive — the contour-free engine for Zagier's telescoping; no closed-contour theorem, no DCT anywhere). Pre-pass scripts/newman_prepass.py ALL PASSED (master ledger f=e^{−t} on the (R,δ,T) grid dps=60, πI sub-identities, weight identity (1+z²/R²)/z = 2Re z/R² on |z|=R, ML sharpness E1–E3, ε-schedule, negative control f=cos t, Mellin bridge with ψ to 10⁴ exact). DISCLOSURES: pure complex-analytic infrastructure — no arithmetic content, no face touched, NOT a §110 event; the port is from a PRIVATE pin proof re-derived on the stadium (public API used where it exists). twin sorry untouched
