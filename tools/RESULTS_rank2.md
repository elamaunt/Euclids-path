# НОВОЕ: матричный ранг, знак four-corner и Cov(r-,r+)

Скрипты: `rank2_harness.py`, `cov_harness.py`, `kappa_harness.py`.

## 1. Матрица счётов N_ij ≈ РАНГ-1 (независимость)
k=24, κ=4: сингулярные числа [764953, 733, 224, 98], sv2/sv1 = 0.001, ранг-1 = 99.9999% энергии.
=> N_ij ≈ u_i v_j (r- ⊥ r+). Для ранг-1 four-corner = РАВЕНСТВО. Значит four-corner = знак ранг-2 поправки.

## 2. Знак ранг-2 поправки (остаток N - rank1), углы́ (k=24):
   (0,0)<0, (0,3)>0, (3,0)>0, (3,3)<0  — ровно паттерн four-corner.
"Двигатель не работает назад на двух точках" = ранг-1 ведущий + знаковый ранг-2 след.

## 3. РОБАСТНОСТЬ ЗНАКА (главное новое)
Тренд R_fc-1 (запас four-corner) и sv2/sv1 по k=20..26, κ=3.6/4.0/4.4: знак ВСЕГДА neg, величина -> 0 (R_fc->1, матрица->ранг1).

Cov(r-,r+) и four-corner D=N00N33-N03N30 по сетке κ∈{3.6,4,4.4,4.8} × k∈{22,24,26}:
  Cov(r-,r+) < 0 на ВСЕХ (k,kappa)  -> True
  four-corner D < 0 на ВСЕХ (k,kappa) -> True
(провал был только при κ=3.3, N33=18 — малая выборка).

## ВЫВОД (новое направление)
Для N33<N00 нужен ТОЛЬКО ЗНАК four-corner (R_fc<=1), запас даёт R_sc~0.014 (устойчиво).
Знак РОБАСТНО отрицателен на всех масштабах => проблема переехала с "контроля величины остатка" (×4, неконтролируема)
на "знак ранг-2 поправки", а он эмпирически устойчив и идёт из ТОЧНОЙ эксклюзивности (shared gcd | 2).
Цель: доказать negative association (знак) из точной per-prime эксклюзивности, distribution-free.
