# RESULTS: аудит two-token standing pump (закрывает ли Step00?)

3 независимых адверсариальных агента, confidence 0.9. Вердикт: **Step00 НЕ закрывается; дыра
переехала и сузилась, но не исчезла.**

## Находки

1. **`same_key_payment` — циркулярно (gap=circular).** Файл (§4-5,§15) делает его истинным ПО
   ОПРЕДЕЛЕНИЮ: `key` объявлен «payment-complete species» ⟹ вывод вшит в определение. Настоящее
   обязательство — предъявить конкретный `NormSig` и ДОКАЗАТЬ коллизия⟹платёж = тот же counting wall
   (fan-in 570→1, RESULTS_global_absorber), не «распаковка определения».

2. **Standing-EPMI не держится.** 2-cycle D₁→D₂→D₁ возвращается ⟹ высота сохраняется ⟹ доказанный
   `no_infinite_engine_descent` (только строго убывающий спуск) к нему НЕприменим (доказано:
   `standing_cycle_preserves_height`). Нужен НОВЫЙ EPMI на lossless-циклах — нет.

3. **Главное противоречие §12 самоликвидируется:** нужно одновременно «Engine опровергается EPMI» И
   «StandingEngine⟹Engine». Под высотным EPMI эти требования ВЗАИМОИСКЛЮЧАЮТ
   (Engine=Desc∨Stand ⟹ EPMI не опровергает; Engine=Desc ⟹ Stand⟹Engine ложно).

4. **2-cycle vs self-loop — косметика:** честно убрал quotient-артефакт, но не связывающее
   ограничение. Обе версии оставляют те же две дыры.

## Остаётся (точные обязательства)

- предъявить конечный `NormSig` + доказать коллизия⟹certified payment БЕЗ циркулярности (= counting wall);
- новый EPMI на height-сохраняющих standing-циклах;
- конкретное `EuclideanEngine`, связанное с доказанным EPMI, чтобы §12 не самоликвидировалась.

Step00 = sorry. Файлы честны (входы помечены недоказанными); это не скрытый дефект, а признанная редукция.
